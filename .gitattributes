# 文本文件统一 LF（避免 Windows 上 CRLF 造成的假 diff）
* text=auto eol=lf

# 二进制：别让 git 尝试 diff / 合并
*.7z binary
*.zip binary
*.rar binary
*.gz binary
*.tgz binary
*.gif binary

# 生成物在 diff / 语言统计里不显示
lib/client.js linguist-generated=true
