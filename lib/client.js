/**
 * 蓝色大肥鱼桌宠 —— Client 半边。
 *
 * 这是模板：`const BUILT_PETS = …` / `const BUILT_ASSETS = …` / `const BUILT_POOLS = …` /
 * `const BASE = …` 四行由 tools/build-client.mjs 用素材目录的真实内容重写，产物是
 * lib/client.js（浏览器实际加载的文件）。改交互改这里，然后重新构建。
 *
 * 这四行只是**兜底数据**：组件挂载后会向 Host 要一份运行时花名册
 * （GET BASE/__roster），拿到就整体替换 —— 所以加新宠物不必重新构建，
 * 在面板里点「导入 / 重扫」即可（Host 和构建脚本共用 lib/catalog.mjs 的扫描逻辑）。
 *
 * 素材靠 Host 半边的 HTTP 路由提供，这里只拼 URL，不搬字节。
 *
 * ── 状态机 ──────────────────────────────────────────────────────────────
 * 世界状态（DSH 给的，来自 shell.overlay 槽的 useSessionStatus 标准 props）：
 *   attention  有会话在等你回答（审批/提问）
 *   running    有会话正在干活
 *   done       有后台会话刚干完、还没看过
 *   idle       什么都没发生
 *
 * 宠物状态（优先级从高到低）：
 *   pin        面板里点选了某个动画 → 一直播它
 *   drag       被拖来拖去 → 紧张/害怕
 *   poke       被点了一下 → 卖萌几秒，然后回到世界状态
 *   celebrate  刚干完活的那一下 → 庆祝几秒
 *   世界状态   attention / running(先思考后打字) / done(通知提示)
 *   idle       过「生活」：睡觉、干饭、唱歌、摸鱼、发呆、搞事、瞎逛，
 *              每段生活持续几十秒到几分钟，而不是一首一首换歌
 */
window.__ModuleLoader__.load({
  id: 'dsh-fish-pet',
  factory(require) {
    const React = require('react')
    const h = React.createElement

    /** 构建时写进来的花名册（离线兜底）：{ id, label, count } */
    const BUILT_PETS = [{"id":"demo-cat","label":"demo-cat","count":15},{"id":"fat-fish","label":"fat-fish","count":157}]
    /** 构建时写进来的动画：{ f: 相对路径, u: URL 片段, n: 显示名, ms: 一轮时长, pet: 属于哪只宠物 } */
    const BUILT_ASSETS = [{"f":"demo-cat/demo-cat_被击中(爱心).gif","u":"demo-cat/demo-cat_%E8%A2%AB%E5%87%BB%E4%B8%AD(%E7%88%B1%E5%BF%83).gif","n":"被击中(爱心)","ms":820,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_唱歌.gif","u":"demo-cat/demo-cat_%E5%94%B1%E6%AD%8C.gif","n":"唱歌","ms":1180,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_吃(西瓜).gif","u":"demo-cat/demo-cat_%E5%90%83(%E8%A5%BF%E7%93%9C).gif","n":"吃(西瓜)","ms":600,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_打字(恼怒).gif","u":"demo-cat/demo-cat_%E6%89%93%E5%AD%97(%E6%81%BC%E6%80%92).gif","n":"打字(恼怒)","ms":600,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_打字(普通).gif","u":"demo-cat/demo-cat_%E6%89%93%E5%AD%97(%E6%99%AE%E9%80%9A).gif","n":"打字(普通)","ms":980,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_呆 1.gif","u":"demo-cat/demo-cat_%E5%91%86%201.gif","n":"呆 1","ms":980,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_带薪拉屎(简单模式).gif","u":"demo-cat/demo-cat_%E5%B8%A6%E8%96%AA%E6%8B%89%E5%B1%8E(%E7%AE%80%E5%8D%95%E6%A8%A1%E5%BC%8F).gif","n":"带薪拉屎(简单模式)","ms":1320,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_害怕 1.gif","u":"demo-cat/demo-cat_%E5%AE%B3%E6%80%95%201.gif","n":"害怕 1","ms":600,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_害羞 1.gif","u":"demo-cat/demo-cat_%E5%AE%B3%E7%BE%9E%201.gif","n":"害羞 1","ms":980,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_期待 1.gif","u":"demo-cat/demo-cat_%E6%9C%9F%E5%BE%85%201.gif","n":"期待 1","ms":2640,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_庆祝.gif","u":"demo-cat/demo-cat_%E5%BA%86%E7%A5%9D.gif","n":"庆祝","ms":1320,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_睡觉(普通).gif","u":"demo-cat/demo-cat_%E7%9D%A1%E8%A7%89(%E6%99%AE%E9%80%9A).gif","n":"睡觉(普通)","ms":2640,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_思考(认真地).gif","u":"demo-cat/demo-cat_%E6%80%9D%E8%80%83(%E8%AE%A4%E7%9C%9F%E5%9C%B0).gif","n":"思考(认真地)","ms":980,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_通知_提示 点赞.gif","u":"demo-cat/demo-cat_%E9%80%9A%E7%9F%A5_%E6%8F%90%E7%A4%BA%20%E7%82%B9%E8%B5%9E.gif","n":"通知_提示 点赞","ms":1720,"pet":"demo-cat"},{"f":"demo-cat/demo-cat_小丑 1.gif","u":"demo-cat/demo-cat_%E5%B0%8F%E4%B8%91%201.gif","n":"小丑 1","ms":2320,"pet":"demo-cat"},{"f":"fat-fish/蓝色大肥鱼_爱心 1_2026-08-18-14-20-15.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%88%B1%E5%BF%83%201_2026-08-18-14-20-15.gif","n":"爱心 1","ms":2140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_爱心 2_2026-08-18-14-20-22.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%88%B1%E5%BF%83%202_2026-08-18-14-20-22.gif","n":"爱心 2","ms":2120,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_爱心 3_2026-08-18-14-20-29.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%88%B1%E5%BF%83%203_2026-08-18-14-20-29.gif","n":"爱心 3","ms":1920,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_按钮_2026-08-18-14-03-53.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8C%89%E9%92%AE_2026-08-18-14-03-53.gif","n":"按钮","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_被击中(爱心)_2026-08-18-14-13-47.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%A2%AB%E5%87%BB%E4%B8%AD(%E7%88%B1%E5%BF%83)_2026-08-18-14-13-47.gif","n":"被击中(爱心)","ms":820,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_被击中(拖鞋)_2026-08-18-14-13-52.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%A2%AB%E5%87%BB%E4%B8%AD(%E6%8B%96%E9%9E%8B)_2026-08-18-14-13-52.gif","n":"被击中(拖鞋)","ms":820,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_被击中(硬币)_2026-08-18-14-13-30.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%A2%AB%E5%87%BB%E4%B8%AD(%E7%A1%AC%E5%B8%81)_2026-08-18-14-13-30.gif","n":"被击中(硬币)","ms":820,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_被击中(Bits)_2026-08-18-14-13-19.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%A2%AB%E5%87%BB%E4%B8%AD(Bits)_2026-08-18-14-13-19.gif","n":"被击中(Bits)","ms":820,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_馋(刀叉)_2026-08-18-14-09-51.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%A6%8B(%E5%88%80%E5%8F%89)_2026-08-18-14-09-51.gif","n":"馋(刀叉)","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_馋(筷子)_2026-08-18-14-09-43.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%A6%8B(%E7%AD%B7%E5%AD%90)_2026-08-18-14-09-43.gif","n":"馋(筷子)","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_唱歌_2026-08-18-14-28-11.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%94%B1%E6%AD%8C_2026-08-18-14-28-11.gif","n":"唱歌","ms":1180,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_吃(爆米花)_2026-08-18-14-11-25.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%90%83(%E7%88%86%E7%B1%B3%E8%8A%B1)_2026-08-18-14-11-25.gif","n":"吃(爆米花)","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_吃(甜甜圈)_2026-08-18-14-11-06.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%90%83(%E7%94%9C%E7%94%9C%E5%9C%88)_2026-08-18-14-11-06.gif","n":"吃(甜甜圈)","ms":3980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_吃(西瓜)_2026-08-18-14-11-30.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%90%83(%E8%A5%BF%E7%93%9C)_2026-08-18-14-11-30.gif","n":"吃(西瓜)","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_催眠_2026-08-18-14-15-43.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%82%AC%E7%9C%A0_2026-08-18-14-15-43.gif","n":"催眠","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_打游戏_2026-08-18-14-13-01.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%89%93%E6%B8%B8%E6%88%8F_2026-08-18-14-13-01.gif","n":"打游戏","ms":780,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_打招呼 1_2026-08-18-14-14-29.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%89%93%E6%8B%9B%E5%91%BC%201_2026-08-18-14-14-29.gif","n":"打招呼 1","ms":2320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_打招呼 2_2026-08-18-14-14-35.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%89%93%E6%8B%9B%E5%91%BC%202_2026-08-18-14-14-35.gif","n":"打招呼 2","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_打字(恼怒)_2026-08-18-14-38-27.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%89%93%E5%AD%97(%E6%81%BC%E6%80%92)_2026-08-18-14-38-27.gif","n":"打字(恼怒)","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_打字(普通)_2026-08-18-14-38-34.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%89%93%E5%AD%97(%E6%99%AE%E9%80%9A)_2026-08-18-14-38-34.gif","n":"打字(普通)","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_打字(生气)_2026-08-18-14-38-24.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%89%93%E5%AD%97(%E7%94%9F%E6%B0%94)_2026-08-18-14-38-24.gif","n":"打字(生气)","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_呆 1_2026-08-18-14-10-05.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%91%86%201_2026-08-18-14-10-05.gif","n":"呆 1","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_呆 2_2026-08-18-14-10-11.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%91%86%202_2026-08-18-14-10-11.gif","n":"呆 2","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_呆 3_2026-08-18-14-10-18.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%91%86%203_2026-08-18-14-10-18.gif","n":"呆 3","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_呆(贴纸) 1_2026-08-18-14-10-26.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%91%86(%E8%B4%B4%E7%BA%B8)%201_2026-08-18-14-10-26.gif","n":"呆(贴纸) 1","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_呆(贴纸) 2_2026-08-18-14-10-34.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%91%86(%E8%B4%B4%E7%BA%B8)%202_2026-08-18-14-10-34.gif","n":"呆(贴纸) 2","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_呆(贴纸) 3_2026-08-18-14-10-39.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%91%86(%E8%B4%B4%E7%BA%B8)%203_2026-08-18-14-10-39.gif","n":"呆(贴纸) 3","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_带薪拉屎(简单模式)_2026-08-18-14-23-31.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B8%A6%E8%96%AA%E6%8B%89%E5%B1%8E(%E7%AE%80%E5%8D%95%E6%A8%A1%E5%BC%8F)_2026-08-18-14-23-31.gif","n":"带薪拉屎(简单模式)","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_带薪拉屎(困难模式)_2026-08-18-14-23-38.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B8%A6%E8%96%AA%E6%8B%89%E5%B1%8E(%E5%9B%B0%E9%9A%BE%E6%A8%A1%E5%BC%8F)_2026-08-18-14-23-38.gif","n":"带薪拉屎(困难模式)","ms":2140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_蛋糕_2026-08-18-14-04-05.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%9B%8B%E7%B3%95_2026-08-18-14-04-05.gif","n":"蛋糕","ms":1520,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_刀 1_2026-08-18-14-16-13.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%88%80%201_2026-08-18-14-16-13.gif","n":"刀 1","ms":1140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_刀 2_2026-08-18-14-16-18.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%88%80%202_2026-08-18-14-16-18.gif","n":"刀 2","ms":1140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_到达_2026-08-18-14-02-26.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%88%B0%E8%BE%BE_2026-08-18-14-02-26.gif","n":"到达","ms":2080,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_到达(拿筷子)_2026-08-18-14-02-54.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%88%B0%E8%BE%BE(%E6%8B%BF%E7%AD%B7%E5%AD%90)_2026-08-18-14-02-54.gif","n":"到达(拿筷子)","ms":2180,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_到达(拿勺子)_2026-08-18-14-03-03.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%88%B0%E8%BE%BE(%E6%8B%BF%E5%8B%BA%E5%AD%90)_2026-08-18-14-03-03.gif","n":"到达(拿勺子)","ms":2180,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_得分(0分)_2026-08-18-14-26-51.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%BE%97%E5%88%86(0%E5%88%86)_2026-08-18-14-26-51.gif","n":"得分(0分)","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_得分(10分)_2026-08-18-14-27-16.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%BE%97%E5%88%86(10%E5%88%86)_2026-08-18-14-27-16.gif","n":"得分(10分)","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_点头_2026-08-18-14-15-19.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%82%B9%E5%A4%B4_2026-08-18-14-15-19.gif","n":"点头","ms":780,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_点赞_2026-08-18-14-38-05.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%82%B9%E8%B5%9E_2026-08-18-14-38-05.gif","n":"点赞","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_电风扇 1_2026-08-18-14-11-56.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%94%B5%E9%A3%8E%E6%89%87%201_2026-08-18-14-11-56.gif","n":"电风扇 1","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_电风扇 2_2026-08-18-14-12-00.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%94%B5%E9%A3%8E%E6%89%87%202_2026-08-18-14-12-00.gif","n":"电风扇 2","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_反向点赞_2026-08-18-14-38-00.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%8F%8D%E5%90%91%E7%82%B9%E8%B5%9E_2026-08-18-14-38-00.gif","n":"反向点赞","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_反转_2026-08-18-14-26-21.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%8F%8D%E8%BD%AC_2026-08-18-14-26-21.gif","n":"反转","ms":1340,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_复活节_2026-08-18-14-10-52.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%A4%8D%E6%B4%BB%E8%8A%82_2026-08-18-14-10-52.gif","n":"复活节","ms":1520,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_干杯_2026-08-18-14-05-31.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B9%B2%E6%9D%AF_2026-08-18-14-05-31.gif","n":"干杯","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_工作(疲倦)_2026-08-18-14-39-19.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B7%A5%E4%BD%9C(%E7%96%B2%E5%80%A6)_2026-08-18-14-39-19.gif","n":"工作(疲倦)","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_工作(普通)_2026-08-18-14-39-12.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B7%A5%E4%BD%9C(%E6%99%AE%E9%80%9A)_2026-08-18-14-39-12.gif","n":"工作(普通)","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_工作(生气)_2026-08-18-14-38-57.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B7%A5%E4%BD%9C(%E7%94%9F%E6%B0%94)_2026-08-18-14-38-57.gif","n":"工作(生气)","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_工作(小睡)_2026-08-18-14-39-08.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B7%A5%E4%BD%9C(%E5%B0%8F%E7%9D%A1)_2026-08-18-14-39-08.gif","n":"工作(小睡)","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_害怕 1_2026-08-18-14-26-34.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%AE%B3%E6%80%95%201_2026-08-18-14-26-34.gif","n":"害怕 1","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_害怕 2_2026-08-18-14-26-44.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%AE%B3%E6%80%95%202_2026-08-18-14-26-44.gif","n":"害怕 2","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_害羞 1_2026-08-18-14-27-24.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%AE%B3%E7%BE%9E%201_2026-08-18-14-27-24.gif","n":"害羞 1","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_害羞 2_2026-08-18-14-27-35.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%AE%B3%E7%BE%9E%202_2026-08-18-14-27-35.gif","n":"害羞 2","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_汗_2026-08-18-14-36-26.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%B1%97_2026-08-18-14-36-26.gif","n":"汗","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_喝(饮料杯)_2026-08-18-14-08-52.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%96%9D(%E9%A5%AE%E6%96%99%E6%9D%AF)_2026-08-18-14-08-52.gif","n":"喝(饮料杯)","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_红包 1_2026-08-18-14-25-33.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%BA%A2%E5%8C%85%201_2026-08-18-14-25-33.gif","n":"红包 1","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_红包 2_2026-08-18-14-25-45.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%BA%A2%E5%8C%85%202_2026-08-18-14-25-45.gif","n":"红包 2","ms":1780,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_画板_2026-08-18-14-36-39.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%94%BB%E6%9D%BF_2026-08-18-14-36-39.gif","n":"画板","ms":780,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_吉他_2026-08-18-14-14-50.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%90%89%E4%BB%96_2026-08-18-14-14-50.gif","n":"吉他","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_记录 1_2026-08-18-14-23-06.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%AE%B0%E5%BD%95%201_2026-08-18-14-23-06.gif","n":"记录 1","ms":2640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_记录 2_2026-08-18-14-23-16.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%AE%B0%E5%BD%95%202_2026-08-18-14-23-16.gif","n":"记录 2","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_加油_2026-08-18-14-05-09.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%8A%A0%E6%B2%B9_2026-08-18-14-05-09.gif","n":"加油","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_驾驶_2026-08-18-14-09-11.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%A9%BE%E9%A9%B6_2026-08-18-14-09-11.gif","n":"驾驶","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_胶带_2026-08-18-14-37-14.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%83%B6%E5%B8%A6_2026-08-18-14-37-14.gif","n":"胶带","ms":1380,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_紧张 1_2026-08-18-14-22-46.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%B4%A7%E5%BC%A0%201_2026-08-18-14-22-46.gif","n":"紧张 1","ms":1180,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_紧张 2_2026-08-18-14-22-53.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%B4%A7%E5%BC%A0%202_2026-08-18-14-22-53.gif","n":"紧张 2","ms":1180,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_惊吓_2026-08-18-14-36-14.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%83%8A%E5%90%93_2026-08-18-14-36-14.gif","n":"惊吓","ms":1140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_静音 1_2026-08-18-14-21-45.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%9D%99%E9%9F%B3%201_2026-08-18-14-21-45.gif","n":"静音 1","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_静音 2_2026-08-18-14-22-18.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%9D%99%E9%9F%B3%202_2026-08-18-14-22-18.gif","n":"静音 2","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_哭 1_2026-08-18-14-07-00.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%93%AD%201_2026-08-18-14-07-00.gif","n":"哭 1","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_哭 2_2026-08-18-14-07-19.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%93%AD%202_2026-08-18-14-07-19.gif","n":"哭 2","ms":1940,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_哭 3_2026-08-18-14-07-23.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%93%AD%203_2026-08-18-14-07-23.gif","n":"哭 3","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_扩音器_2026-08-18-14-21-17.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%89%A9%E9%9F%B3%E5%99%A8_2026-08-18-14-21-17.gif","n":"扩音器","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_垃圾桶_2026-08-18-14-38-13.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%9E%83%E5%9C%BE%E6%A1%B6_2026-08-18-14-38-13.gif","n":"垃圾桶","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_礼物 1_2026-08-18-14-14-11.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%A4%BC%E7%89%A9%201_2026-08-18-14-14-11.gif","n":"礼物 1","ms":1720,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_礼物 2_2026-08-18-14-14-21.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%A4%BC%E7%89%A9%202_2026-08-18-14-14-21.gif","n":"礼物 2","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_六七_2026-08-18-14-28-24.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%85%AD%E4%B8%83_2026-08-18-14-28-24.gif","n":"六七","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_六七(呆)_2026-08-18-14-28-28.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%85%AD%E4%B8%83(%E5%91%86)_2026-08-18-14-28-28.gif","n":"六七(呆)","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_冒泡 1_2026-08-18-14-19-35.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%86%92%E6%B3%A1%201_2026-08-18-14-19-35.gif","n":"冒泡 1","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_冒泡 2_2026-08-18-14-19-43.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%86%92%E6%B3%A1%202_2026-08-18-14-19-43.gif","n":"冒泡 2","ms":2320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_玫瑰_2026-08-18-14-26-27.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%8E%AB%E7%91%B0_2026-08-18-14-26-27.gif","n":"玫瑰","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_摸头_2026-08-18-14-23-48.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%91%B8%E5%A4%B4_2026-08-18-14-23-48.gif","n":"摸头","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_魔法_2026-08-18-14-20-35.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%AD%94%E6%B3%95_2026-08-18-14-20-35.gif","n":"魔法","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_墨镜反光_2026-08-18-14-35-45.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%A2%A8%E9%95%9C%E5%8F%8D%E5%85%89_2026-08-18-14-35-45.gif","n":"墨镜反光","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_墨镜循环_2026-08-18-14-35-52.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%A2%A8%E9%95%9C%E5%BE%AA%E7%8E%AF_2026-08-18-14-35-52.gif","n":"墨镜循环","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_拿走我的钱_2026-08-18-14-37-03.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8B%BF%E8%B5%B0%E6%88%91%E7%9A%84%E9%92%B1_2026-08-18-14-37-03.gif","n":"拿走我的钱","ms":1140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_奶酪糊脸_2026-08-18-14-05-39.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%A5%B6%E9%85%AA%E7%B3%8A%E8%84%B8_2026-08-18-14-05-39.gif","n":"奶酪糊脸","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_拍蝇_2026-08-18-14-36-19.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8B%8D%E8%9D%87_2026-08-18-14-36-19.gif","n":"拍蝇","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_喷剂_2026-08-18-14-29-52.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%96%B7%E5%89%82_2026-08-18-14-29-52.gif","n":"喷剂","ms":1380,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_期待 1_2026-08-18-14-29-36.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%9C%9F%E5%BE%85%201_2026-08-18-14-29-36.gif","n":"期待 1","ms":2640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_期待 2_2026-08-18-14-29-41.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%9C%9F%E5%BE%85%202_2026-08-18-14-29-41.gif","n":"期待 2","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_钱_2026-08-18-14-21-34.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%92%B1_2026-08-18-14-21-34.gif","n":"钱","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_枪_2026-08-18-14-15-10.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%9E%AA_2026-08-18-14-15-10.gif","n":"枪","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_敲头_2026-08-18-14-03-25.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%95%B2%E5%A4%B4_2026-08-18-14-03-25.gif","n":"敲头","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_撬棍 1_2026-08-18-14-06-29.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%92%AC%E6%A3%8D%201_2026-08-18-14-06-29.gif","n":"撬棍 1","ms":1140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_撬棍 2_2026-08-18-14-06-36.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%92%AC%E6%A3%8D%202_2026-08-18-14-06-36.gif","n":"撬棍 2","ms":1480,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_撬棍 3_2026-08-18-14-06-48.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%92%AC%E6%A3%8D%203_2026-08-18-14-06-48.gif","n":"撬棍 3","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_情书_2026-08-18-14-19-12.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%83%85%E4%B9%A6_2026-08-18-14-19-12.gif","n":"情书","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_庆祝_2026-08-18-14-04-58.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%BA%86%E7%A5%9D_2026-08-18-14-04-58.gif","n":"庆祝","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_烧 1_2026-08-18-14-12-34.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%83%A7%201_2026-08-18-14-12-34.gif","n":"烧 1","ms":1580,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_烧 2_2026-08-18-14-12-42.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%83%A7%202_2026-08-18-14-12-42.gif","n":"烧 2","ms":1580,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_生气_2026-08-18-14-02-17.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%94%9F%E6%B0%94_2026-08-18-14-02-17.gif","n":"生气","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_刷卡_2026-08-18-14-04-14.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%88%B7%E5%8D%A1_2026-08-18-14-04-14.gif","n":"刷卡","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_睡觉 (UU)_2026-08-18-14-29-04.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%9D%A1%E8%A7%89%20(UU)_2026-08-18-14-29-04.gif","n":"睡觉 (UU)","ms":2640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_睡觉(普通)_2026-08-18-14-28-41.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%9D%A1%E8%A7%89(%E6%99%AE%E9%80%9A)_2026-08-18-14-28-41.gif","n":"睡觉(普通)","ms":2640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_睡觉(准备阶段1)_2026-08-18-14-28-50.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%9D%A1%E8%A7%89(%E5%87%86%E5%A4%87%E9%98%B6%E6%AE%B51)_2026-08-18-14-28-50.gif","n":"睡觉(准备阶段1)","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_睡觉(准备阶段2)_2026-08-18-14-28-57.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%9D%A1%E8%A7%89(%E5%87%86%E5%A4%87%E9%98%B6%E6%AE%B52)_2026-08-18-14-28-57.gif","n":"睡觉(准备阶段2)","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_思考(认真地)_2026-08-18-14-37-25.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%80%9D%E8%80%83(%E8%AE%A4%E7%9C%9F%E5%9C%B0)_2026-08-18-14-37-25.gif","n":"思考(认真地)","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_思考(自信地)_2026-08-18-14-37-20.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%80%9D%E8%80%83(%E8%87%AA%E4%BF%A1%E5%9C%B0)_2026-08-18-14-37-20.gif","n":"思考(自信地)","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_死亡_2026-08-18-14-08-28.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%AD%BB%E4%BA%A1_2026-08-18-14-08-28.gif","n":"死亡","ms":1380,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_叹号_2026-08-18-14-12-08.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%8F%B9%E5%8F%B7_2026-08-18-14-12-08.gif","n":"叹号","ms":1140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_舔舔_2026-08-18-14-16-54.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%88%94%E8%88%94_2026-08-18-14-16-54.gif","n":"舔舔","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_跳舞 1_2026-08-18-14-07-32.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%B7%B3%E8%88%9E%201_2026-08-18-14-07-32.gif","n":"跳舞 1","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_跳舞(低皮质醇)_2026-08-18-14-08-05.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%B7%B3%E8%88%9E(%E4%BD%8E%E7%9A%AE%E8%B4%A8%E9%86%87)_2026-08-18-14-08-05.gif","n":"跳舞(低皮质醇)","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_跳舞(Caramelldansen)_2026-08-18-14-07-42.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%B7%B3%E8%88%9E(Caramelldansen)_2026-08-18-14-07-42.gif","n":"跳舞(Caramelldansen)","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_跳舞(Helltaker)_2026-08-18-14-07-49.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%B7%B3%E8%88%9E(Helltaker)_2026-08-18-14-07-49.gif","n":"跳舞(Helltaker)","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_停止工作_2026-08-18-14-34-45.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%81%9C%E6%AD%A2%E5%B7%A5%E4%BD%9C_2026-08-18-14-34-45.gif","n":"停止工作","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_通知_提示 爱心_喜欢_2026-08-18-14-01-51.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%80%9A%E7%9F%A5_%E6%8F%90%E7%A4%BA%20%E7%88%B1%E5%BF%83_%E5%96%9C%E6%AC%A2_2026-08-18-14-01-51.gif","n":"通知_提示 爱心_喜欢","ms":1720,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_通知_提示 点赞_2026-08-18-14-01-58.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%80%9A%E7%9F%A5_%E6%8F%90%E7%A4%BA%20%E7%82%B9%E8%B5%9E_2026-08-18-14-01-58.gif","n":"通知_提示 点赞","ms":1720,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_通知_提示 星星_收藏_2026-08-18-14-02-04.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%80%9A%E7%9F%A5_%E6%8F%90%E7%A4%BA%20%E6%98%9F%E6%98%9F_%E6%94%B6%E8%97%8F_2026-08-18-14-02-04.gif","n":"通知_提示 星星_收藏","ms":1720,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_通知_提示 硬币_2026-08-18-14-01-45.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%80%9A%E7%9F%A5_%E6%8F%90%E7%A4%BA%20%E7%A1%AC%E5%B8%81_2026-08-18-14-01-45.gif","n":"通知_提示 硬币","ms":1720,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_通知_提示 Bits_2026-08-18-14-01-30.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%80%9A%E7%9F%A5_%E6%8F%90%E7%A4%BA%20Bits_2026-08-18-14-01-30.gif","n":"通知_提示 Bits","ms":1720,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_通知_自定义_2026-08-18-14-01-38.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%80%9A%E7%9F%A5_%E8%87%AA%E5%AE%9A%E4%B9%89_2026-08-18-14-01-38.gif","n":"通知_自定义","ms":1720,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_头晕_2026-08-18-14-08-36.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%A4%B4%E6%99%95_2026-08-18-14-08-36.gif","n":"头晕","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_拖鞋 1_2026-08-18-14-29-10.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8B%96%E9%9E%8B%201_2026-08-18-14-29-10.gif","n":"拖鞋 1","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_拖鞋 2_2026-08-18-14-29-22.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8B%96%E9%9E%8B%202_2026-08-18-14-29-22.gif","n":"拖鞋 2","ms":2320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_问号_2026-08-18-14-25-05.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E9%97%AE%E5%8F%B7_2026-08-18-14-25-05.gif","n":"问号","ms":1140,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_舞蹈(散味)_2026-08-18-14-08-18.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%88%9E%E8%B9%88(%E6%95%A3%E5%91%B3)_2026-08-18-14-08-18.gif","n":"舞蹈(散味)","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_像素墨镜反光_2026-08-18-14-35-59.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%83%8F%E7%B4%A0%E5%A2%A8%E9%95%9C%E5%8F%8D%E5%85%89_2026-08-18-14-35-59.gif","n":"像素墨镜反光","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_小丑 1_2026-08-18-14-05-49.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B0%8F%E4%B8%91%201_2026-08-18-14-05-49.gif","n":"小丑 1","ms":2320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_小丑 2_2026-08-18-14-05-56.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%B0%8F%E4%B8%91%202_2026-08-18-14-05-56.gif","n":"小丑 2","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_笑_2026-08-18-14-16-28.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%AC%91_2026-08-18-14-16-28.gif","n":"笑","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_笑(指)_2026-08-18-14-16-39.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%AC%91(%E6%8C%87)_2026-08-18-14-16-39.gif","n":"笑(指)","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_摇可乐_2026-08-18-14-06-00.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%91%87%E5%8F%AF%E4%B9%90_2026-08-18-14-06-00.gif","n":"摇可乐","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_摇铃_2026-08-18-14-03-15.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%91%87%E9%93%83_2026-08-18-14-03-15.gif","n":"摇铃","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_摇头_2026-08-18-14-15-27.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%91%87%E5%A4%B4_2026-08-18-14-15-27.gif","n":"摇头","ms":640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_要米_2026-08-18-14-22-34.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%A6%81%E7%B1%B3_2026-08-18-14-22-34.gif","n":"要米","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_一切都好 1_2026-08-18-14-37-38.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E4%B8%80%E5%88%87%E9%83%BD%E5%A5%BD%201_2026-08-18-14-37-38.gif","n":"一切都好 1","ms":1580,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_一切都好 2_2026-08-18-14-37-44.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E4%B8%80%E5%88%87%E9%83%BD%E5%A5%BD%202_2026-08-18-14-37-44.gif","n":"一切都好 2","ms":1580,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_一切都好 3_2026-08-18-14-37-50.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E4%B8%80%E5%88%87%E9%83%BD%E5%A5%BD%203_2026-08-18-14-37-50.gif","n":"一切都好 3","ms":1580,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_荧光棒 2_2026-08-18-14-18-47.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%8D%A7%E5%85%89%E6%A3%92%202_2026-08-18-14-18-47.gif","n":"荧光棒 2","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_眨眼_2026-08-18-14-38-44.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E7%9C%A8%E7%9C%BC_2026-08-18-14-38-44.gif","n":"眨眼","ms":1480,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_摘掉墨镜_2026-08-18-14-36-07.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%91%98%E6%8E%89%E5%A2%A8%E9%95%9C_2026-08-18-14-36-07.gif","n":"摘掉墨镜","ms":1920,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_折扇_2026-08-18-14-12-51.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8A%98%E6%89%87_2026-08-18-14-12-51.gif","n":"折扇","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_正在思考_2026-08-18-14-24-28.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%AD%A3%E5%9C%A8%E6%80%9D%E8%80%83_2026-08-18-14-24-28.gif","n":"正在思考","ms":980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_指_2026-08-18-14-24-46.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8C%87_2026-08-18-14-24-46.gif","n":"指","ms":780,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_主意_2026-08-18-14-15-51.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E4%B8%BB%E6%84%8F_2026-08-18-14-15-51.gif","n":"主意","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_抓拍(摄像机)_2026-08-18-14-04-46.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8A%93%E6%8B%8D(%E6%91%84%E5%83%8F%E6%9C%BA)_2026-08-18-14-04-46.gif","n":"抓拍(摄像机)","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_抓拍(手机)_2026-08-18-14-04-40.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E6%8A%93%E6%8B%8D(%E6%89%8B%E6%9C%BA)_2026-08-18-14-04-40.gif","n":"抓拍(手机)","ms":1640,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_自我安慰_2026-08-18-14-06-15.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E8%87%AA%E6%88%91%E5%AE%89%E6%85%B0_2026-08-18-14-06-15.gif","n":"自我安慰","ms":1840,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_坐牢 1_2026-08-18-14-16-02.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%9D%90%E7%89%A2%201_2026-08-18-14-16-02.gif","n":"坐牢 1","ms":1240,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_坐牢 2_2026-08-18-14-16-04.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_%E5%9D%90%E7%89%A2%202_2026-08-18-14-16-04.gif","n":"坐牢 2","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_Bug_2026-08-18-14-03-46.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_Bug_2026-08-18-14-03-46.gif","n":"Bug","ms":1980,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_Popcat 平滑_2026-08-18-14-24-53.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_Popcat%20%E5%B9%B3%E6%BB%91_2026-08-18-14-24-53.gif","n":"Popcat 平滑","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_Popcat 帧_2026-08-18-14-24-51.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_Popcat%20%E5%B8%A7_2026-08-18-14-24-51.gif","n":"Popcat 帧","ms":600,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_Raid 1_2026-08-18-14-25-14.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_Raid%201_2026-08-18-14-25-14.gif","n":"Raid 1","ms":1320,"pet":"fat-fish"},{"f":"fat-fish/蓝色大肥鱼_Raid 2_2026-08-18-14-25-25.gif","u":"fat-fish/%E8%93%9D%E8%89%B2%E5%A4%A7%E8%82%A5%E9%B1%BC_Raid%202_2026-08-18-14-25-25.gif","n":"Raid 2","ms":2080,"pet":"fat-fish"}]
    /** 构建时写进来的池子：petId → { 池名: [f, ...] } */
    const BUILT_POOLS = {"demo-cat":{"think":["demo-cat/demo-cat_思考(认真地).gif"],"work":["demo-cat/demo-cat_打字(普通).gif","demo-cat/demo-cat_打字(恼怒).gif"],"attention":["demo-cat/demo-cat_期待 1.gif"],"celebrate":["demo-cat/demo-cat_庆祝.gif","demo-cat/demo-cat_唱歌.gif"],"notify":["demo-cat/demo-cat_通知_提示 点赞.gif"],"moe":["demo-cat/demo-cat_害羞 1.gif","demo-cat/demo-cat_期待 1.gif"],"hold":["demo-cat/demo-cat_害怕 1.gif"],"drop":["demo-cat/demo-cat_被击中(爱心).gif"],"sleep":["demo-cat/demo-cat_睡觉(普通).gif"],"eat":["demo-cat/demo-cat_吃(西瓜).gif"],"sing":["demo-cat/demo-cat_唱歌.gif"],"slack":["demo-cat/demo-cat_带薪拉屎(简单模式).gif"],"daze":["demo-cat/demo-cat_呆 1.gif"],"mischief":["demo-cat/demo-cat_小丑 1.gif"],"roam":["demo-cat/demo-cat_被击中(爱心).gif","demo-cat/demo-cat_唱歌.gif","demo-cat/demo-cat_吃(西瓜).gif","demo-cat/demo-cat_打字(恼怒).gif","demo-cat/demo-cat_打字(普通).gif","demo-cat/demo-cat_呆 1.gif","demo-cat/demo-cat_带薪拉屎(简单模式).gif","demo-cat/demo-cat_害怕 1.gif"]},"fat-fish":{"think":["fat-fish/蓝色大肥鱼_思考(认真地)_2026-08-18-14-37-25.gif","fat-fish/蓝色大肥鱼_思考(自信地)_2026-08-18-14-37-20.gif","fat-fish/蓝色大肥鱼_正在思考_2026-08-18-14-24-28.gif","fat-fish/蓝色大肥鱼_主意_2026-08-18-14-15-51.gif","fat-fish/蓝色大肥鱼_记录 1_2026-08-18-14-23-06.gif"],"work":["fat-fish/蓝色大肥鱼_打字(普通)_2026-08-18-14-38-34.gif","fat-fish/蓝色大肥鱼_打字(恼怒)_2026-08-18-14-38-27.gif","fat-fish/蓝色大肥鱼_打字(生气)_2026-08-18-14-38-24.gif","fat-fish/蓝色大肥鱼_工作(普通)_2026-08-18-14-39-12.gif","fat-fish/蓝色大肥鱼_工作(生气)_2026-08-18-14-38-57.gif","fat-fish/蓝色大肥鱼_工作(疲倦)_2026-08-18-14-39-19.gif","fat-fish/蓝色大肥鱼_撬棍 1_2026-08-18-14-06-29.gif","fat-fish/蓝色大肥鱼_撬棍 2_2026-08-18-14-06-36.gif","fat-fish/蓝色大肥鱼_撬棍 3_2026-08-18-14-06-48.gif","fat-fish/蓝色大肥鱼_魔法_2026-08-18-14-20-35.gif","fat-fish/蓝色大肥鱼_敲头_2026-08-18-14-03-25.gif","fat-fish/蓝色大肥鱼_刷卡_2026-08-18-14-04-14.gif","fat-fish/蓝色大肥鱼_画板_2026-08-18-14-36-39.gif","fat-fish/蓝色大肥鱼_电风扇 1_2026-08-18-14-11-56.gif","fat-fish/蓝色大肥鱼_电风扇 2_2026-08-18-14-12-00.gif","fat-fish/蓝色大肥鱼_喷剂_2026-08-18-14-29-52.gif","fat-fish/蓝色大肥鱼_胶带_2026-08-18-14-37-14.gif","fat-fish/蓝色大肥鱼_拍蝇_2026-08-18-14-36-19.gif","fat-fish/蓝色大肥鱼_烧 1_2026-08-18-14-12-34.gif","fat-fish/蓝色大肥鱼_烧 2_2026-08-18-14-12-42.gif","fat-fish/蓝色大肥鱼_刀 1_2026-08-18-14-16-13.gif","fat-fish/蓝色大肥鱼_刀 2_2026-08-18-14-16-18.gif","fat-fish/蓝色大肥鱼_枪_2026-08-18-14-15-10.gif","fat-fish/蓝色大肥鱼_坐牢 1_2026-08-18-14-16-02.gif","fat-fish/蓝色大肥鱼_坐牢 2_2026-08-18-14-16-04.gif","fat-fish/蓝色大肥鱼_停止工作_2026-08-18-14-34-45.gif"],"attention":["fat-fish/蓝色大肥鱼_期待 1_2026-08-18-14-29-36.gif","fat-fish/蓝色大肥鱼_期待 2_2026-08-18-14-29-41.gif","fat-fish/蓝色大肥鱼_问号_2026-08-18-14-25-05.gif","fat-fish/蓝色大肥鱼_打招呼 1_2026-08-18-14-14-29.gif","fat-fish/蓝色大肥鱼_打招呼 2_2026-08-18-14-14-35.gif","fat-fish/蓝色大肥鱼_摇铃_2026-08-18-14-03-15.gif","fat-fish/蓝色大肥鱼_扩音器_2026-08-18-14-21-17.gif","fat-fish/蓝色大肥鱼_点头_2026-08-18-14-15-19.gif","fat-fish/蓝色大肥鱼_指_2026-08-18-14-24-46.gif","fat-fish/蓝色大肥鱼_紧张 1_2026-08-18-14-22-46.gif","fat-fish/蓝色大肥鱼_紧张 2_2026-08-18-14-22-53.gif"],"celebrate":["fat-fish/蓝色大肥鱼_庆祝_2026-08-18-14-04-58.gif","fat-fish/蓝色大肥鱼_点赞_2026-08-18-14-38-05.gif","fat-fish/蓝色大肥鱼_干杯_2026-08-18-14-05-31.gif","fat-fish/蓝色大肥鱼_加油_2026-08-18-14-05-09.gif","fat-fish/蓝色大肥鱼_玫瑰_2026-08-18-14-26-27.gif","fat-fish/蓝色大肥鱼_爱心 1_2026-08-18-14-20-15.gif","fat-fish/蓝色大肥鱼_爱心 2_2026-08-18-14-20-22.gif","fat-fish/蓝色大肥鱼_爱心 3_2026-08-18-14-20-29.gif","fat-fish/蓝色大肥鱼_得分(10分)_2026-08-18-14-27-16.gif","fat-fish/蓝色大肥鱼_复活节_2026-08-18-14-10-52.gif","fat-fish/蓝色大肥鱼_唱歌_2026-08-18-14-28-11.gif","fat-fish/蓝色大肥鱼_跳舞 1_2026-08-18-14-07-32.gif","fat-fish/蓝色大肥鱼_摇铃_2026-08-18-14-03-15.gif"],"notify":["fat-fish/蓝色大肥鱼_通知_提示 爱心_喜欢_2026-08-18-14-01-51.gif","fat-fish/蓝色大肥鱼_通知_提示 点赞_2026-08-18-14-01-58.gif","fat-fish/蓝色大肥鱼_通知_提示 星星_收藏_2026-08-18-14-02-04.gif","fat-fish/蓝色大肥鱼_通知_提示 硬币_2026-08-18-14-01-45.gif","fat-fish/蓝色大肥鱼_通知_提示 Bits_2026-08-18-14-01-30.gif","fat-fish/蓝色大肥鱼_通知_自定义_2026-08-18-14-01-38.gif","fat-fish/蓝色大肥鱼_摇铃_2026-08-18-14-03-15.gif","fat-fish/蓝色大肥鱼_扩音器_2026-08-18-14-21-17.gif","fat-fish/蓝色大肥鱼_红包 1_2026-08-18-14-25-33.gif","fat-fish/蓝色大肥鱼_红包 2_2026-08-18-14-25-45.gif","fat-fish/蓝色大肥鱼_礼物 1_2026-08-18-14-14-11.gif","fat-fish/蓝色大肥鱼_礼物 2_2026-08-18-14-14-21.gif"],"moe":["fat-fish/蓝色大肥鱼_害羞 1_2026-08-18-14-27-24.gif","fat-fish/蓝色大肥鱼_害羞 2_2026-08-18-14-27-35.gif","fat-fish/蓝色大肥鱼_爱心 1_2026-08-18-14-20-15.gif","fat-fish/蓝色大肥鱼_爱心 2_2026-08-18-14-20-22.gif","fat-fish/蓝色大肥鱼_爱心 3_2026-08-18-14-20-29.gif","fat-fish/蓝色大肥鱼_点赞_2026-08-18-14-38-05.gif","fat-fish/蓝色大肥鱼_反向点赞_2026-08-18-14-38-00.gif","fat-fish/蓝色大肥鱼_舔舔_2026-08-18-14-16-54.gif","fat-fish/蓝色大肥鱼_眨眼_2026-08-18-14-38-44.gif","fat-fish/蓝色大肥鱼_摸头_2026-08-18-14-23-48.gif","fat-fish/蓝色大肥鱼_点头_2026-08-18-14-15-19.gif","fat-fish/蓝色大肥鱼_玫瑰_2026-08-18-14-26-27.gif","fat-fish/蓝色大肥鱼_期待 1_2026-08-18-14-29-36.gif","fat-fish/蓝色大肥鱼_加油_2026-08-18-14-05-09.gif"],"hold":["fat-fish/蓝色大肥鱼_害怕 1_2026-08-18-14-26-34.gif","fat-fish/蓝色大肥鱼_害怕 2_2026-08-18-14-26-44.gif","fat-fish/蓝色大肥鱼_惊吓_2026-08-18-14-36-14.gif","fat-fish/蓝色大肥鱼_紧张 1_2026-08-18-14-22-46.gif","fat-fish/蓝色大肥鱼_紧张 2_2026-08-18-14-22-53.gif","fat-fish/蓝色大肥鱼_汗_2026-08-18-14-36-26.gif","fat-fish/蓝色大肥鱼_头晕_2026-08-18-14-08-36.gif","fat-fish/蓝色大肥鱼_问号_2026-08-18-14-25-05.gif"],"drop":["fat-fish/蓝色大肥鱼_被击中(拖鞋)_2026-08-18-14-13-52.gif","fat-fish/蓝色大肥鱼_被击中(爱心)_2026-08-18-14-13-47.gif","fat-fish/蓝色大肥鱼_被击中(硬币)_2026-08-18-14-13-30.gif","fat-fish/蓝色大肥鱼_被击中(Bits)_2026-08-18-14-13-19.gif","fat-fish/蓝色大肥鱼_惊吓_2026-08-18-14-36-14.gif","fat-fish/蓝色大肥鱼_头晕_2026-08-18-14-08-36.gif","fat-fish/蓝色大肥鱼_生气_2026-08-18-14-02-17.gif","fat-fish/蓝色大肥鱼_反转_2026-08-18-14-26-21.gif","fat-fish/蓝色大肥鱼_死亡_2026-08-18-14-08-28.gif","fat-fish/蓝色大肥鱼_自我安慰_2026-08-18-14-06-15.gif"],"sleep":["fat-fish/蓝色大肥鱼_睡觉(准备阶段1)_2026-08-18-14-28-50.gif","fat-fish/蓝色大肥鱼_睡觉(准备阶段2)_2026-08-18-14-28-57.gif","fat-fish/蓝色大肥鱼_睡觉(普通)_2026-08-18-14-28-41.gif","fat-fish/蓝色大肥鱼_睡觉 (UU)_2026-08-18-14-29-04.gif","fat-fish/蓝色大肥鱼_工作(小睡)_2026-08-18-14-39-08.gif"],"eat":["fat-fish/蓝色大肥鱼_吃(甜甜圈)_2026-08-18-14-11-06.gif","fat-fish/蓝色大肥鱼_吃(西瓜)_2026-08-18-14-11-30.gif","fat-fish/蓝色大肥鱼_吃(爆米花)_2026-08-18-14-11-25.gif","fat-fish/蓝色大肥鱼_馋(刀叉)_2026-08-18-14-09-51.gif","fat-fish/蓝色大肥鱼_馋(筷子)_2026-08-18-14-09-43.gif","fat-fish/蓝色大肥鱼_喝(饮料杯)_2026-08-18-14-08-52.gif","fat-fish/蓝色大肥鱼_蛋糕_2026-08-18-14-04-05.gif","fat-fish/蓝色大肥鱼_干杯_2026-08-18-14-05-31.gif"],"sing":["fat-fish/蓝色大肥鱼_唱歌_2026-08-18-14-28-11.gif","fat-fish/蓝色大肥鱼_吉他_2026-08-18-14-14-50.gif","fat-fish/蓝色大肥鱼_跳舞 1_2026-08-18-14-07-32.gif","fat-fish/蓝色大肥鱼_跳舞(Caramelldansen)_2026-08-18-14-07-42.gif","fat-fish/蓝色大肥鱼_跳舞(Helltaker)_2026-08-18-14-07-49.gif","fat-fish/蓝色大肥鱼_跳舞(低皮质醇)_2026-08-18-14-08-05.gif","fat-fish/蓝色大肥鱼_荧光棒 2_2026-08-18-14-18-47.gif","fat-fish/蓝色大肥鱼_舞蹈(散味)_2026-08-18-14-08-18.gif","fat-fish/蓝色大肥鱼_摇铃_2026-08-18-14-03-15.gif"],"slack":["fat-fish/蓝色大肥鱼_带薪拉屎(简单模式)_2026-08-18-14-23-31.gif","fat-fish/蓝色大肥鱼_带薪拉屎(困难模式)_2026-08-18-14-23-38.gif","fat-fish/蓝色大肥鱼_打游戏_2026-08-18-14-13-01.gif","fat-fish/蓝色大肥鱼_静音 1_2026-08-18-14-21-45.gif","fat-fish/蓝色大肥鱼_静音 2_2026-08-18-14-22-18.gif","fat-fish/蓝色大肥鱼_驾驶_2026-08-18-14-09-11.gif","fat-fish/蓝色大肥鱼_记录 2_2026-08-18-14-23-16.gif","fat-fish/蓝色大肥鱼_画板_2026-08-18-14-36-39.gif","fat-fish/蓝色大肥鱼_电风扇 1_2026-08-18-14-11-56.gif","fat-fish/蓝色大肥鱼_电风扇 2_2026-08-18-14-12-00.gif","fat-fish/蓝色大肥鱼_摇可乐_2026-08-18-14-06-00.gif","fat-fish/蓝色大肥鱼_自我安慰_2026-08-18-14-06-15.gif"],"daze":["fat-fish/蓝色大肥鱼_呆 1_2026-08-18-14-10-05.gif","fat-fish/蓝色大肥鱼_呆 2_2026-08-18-14-10-11.gif","fat-fish/蓝色大肥鱼_呆 3_2026-08-18-14-10-18.gif","fat-fish/蓝色大肥鱼_呆(贴纸) 1_2026-08-18-14-10-26.gif","fat-fish/蓝色大肥鱼_呆(贴纸) 2_2026-08-18-14-10-34.gif","fat-fish/蓝色大肥鱼_呆(贴纸) 3_2026-08-18-14-10-39.gif","fat-fish/蓝色大肥鱼_六七_2026-08-18-14-28-24.gif","fat-fish/蓝色大肥鱼_六七(呆)_2026-08-18-14-28-28.gif","fat-fish/蓝色大肥鱼_冒泡 1_2026-08-18-14-19-35.gif","fat-fish/蓝色大肥鱼_冒泡 2_2026-08-18-14-19-43.gif","fat-fish/蓝色大肥鱼_眨眼_2026-08-18-14-38-44.gif","fat-fish/蓝色大肥鱼_舔舔_2026-08-18-14-16-54.gif","fat-fish/蓝色大肥鱼_Popcat 平滑_2026-08-18-14-24-53.gif","fat-fish/蓝色大肥鱼_Popcat 帧_2026-08-18-14-24-51.gif","fat-fish/蓝色大肥鱼_一切都好 1_2026-08-18-14-37-38.gif","fat-fish/蓝色大肥鱼_一切都好 2_2026-08-18-14-37-44.gif","fat-fish/蓝色大肥鱼_一切都好 3_2026-08-18-14-37-50.gif","fat-fish/蓝色大肥鱼_摇头_2026-08-18-14-15-27.gif","fat-fish/蓝色大肥鱼_点头_2026-08-18-14-15-19.gif"],"mischief":["fat-fish/蓝色大肥鱼_小丑 1_2026-08-18-14-05-49.gif","fat-fish/蓝色大肥鱼_小丑 2_2026-08-18-14-05-56.gif","fat-fish/蓝色大肥鱼_拍蝇_2026-08-18-14-36-19.gif","fat-fish/蓝色大肥鱼_拖鞋 1_2026-08-18-14-29-10.gif","fat-fish/蓝色大肥鱼_拖鞋 2_2026-08-18-14-29-22.gif","fat-fish/蓝色大肥鱼_胶带_2026-08-18-14-37-14.gif","fat-fish/蓝色大肥鱼_垃圾桶_2026-08-18-14-38-13.gif","fat-fish/蓝色大肥鱼_按钮_2026-08-18-14-03-53.gif","fat-fish/蓝色大肥鱼_摇可乐_2026-08-18-14-06-00.gif","fat-fish/蓝色大肥鱼_墨镜循环_2026-08-18-14-35-52.gif","fat-fish/蓝色大肥鱼_墨镜反光_2026-08-18-14-35-45.gif","fat-fish/蓝色大肥鱼_摘掉墨镜_2026-08-18-14-36-07.gif","fat-fish/蓝色大肥鱼_像素墨镜反光_2026-08-18-14-35-59.gif","fat-fish/蓝色大肥鱼_折扇_2026-08-18-14-12-51.gif","fat-fish/蓝色大肥鱼_要米_2026-08-18-14-22-34.gif","fat-fish/蓝色大肥鱼_拿走我的钱_2026-08-18-14-37-03.gif","fat-fish/蓝色大肥鱼_钱_2026-08-18-14-21-34.gif","fat-fish/蓝色大肥鱼_主意_2026-08-18-14-15-51.gif","fat-fish/蓝色大肥鱼_反转_2026-08-18-14-26-21.gif","fat-fish/蓝色大肥鱼_到达_2026-08-18-14-02-26.gif","fat-fish/蓝色大肥鱼_到达(拿筷子)_2026-08-18-14-02-54.gif","fat-fish/蓝色大肥鱼_到达(拿勺子)_2026-08-18-14-03-03.gif","fat-fish/蓝色大肥鱼_抓拍(手机)_2026-08-18-14-04-40.gif","fat-fish/蓝色大肥鱼_抓拍(摄像机)_2026-08-18-14-04-46.gif","fat-fish/蓝色大肥鱼_情书_2026-08-18-14-19-12.gif","fat-fish/蓝色大肥鱼_催眠_2026-08-18-14-15-43.gif"],"roam":["fat-fish/蓝色大肥鱼_得分(0分)_2026-08-18-14-26-51.gif","fat-fish/蓝色大肥鱼_哭 1_2026-08-18-14-07-00.gif","fat-fish/蓝色大肥鱼_哭 2_2026-08-18-14-07-19.gif","fat-fish/蓝色大肥鱼_哭 3_2026-08-18-14-07-23.gif","fat-fish/蓝色大肥鱼_奶酪糊脸_2026-08-18-14-05-39.gif","fat-fish/蓝色大肥鱼_叹号_2026-08-18-14-12-08.gif","fat-fish/蓝色大肥鱼_笑_2026-08-18-14-16-28.gif","fat-fish/蓝色大肥鱼_笑(指)_2026-08-18-14-16-39.gif","fat-fish/蓝色大肥鱼_Bug_2026-08-18-14-03-46.gif","fat-fish/蓝色大肥鱼_Raid 1_2026-08-18-14-25-14.gif","fat-fish/蓝色大肥鱼_Raid 2_2026-08-18-14-25-25.gif"]}}
    const BASE = "/fish-pet"
    const STORAGE_KEY = 'dsh-fish-pet:v1'

    /**
     * 台词库：池名 → 台词数组。
     * 池名和状态机的状态一一对应（think/work/attention/done + 七种「生活」），
     * 另外 poke/hold/drop/pin/show/muse 是交互和自言自语用的。
     * `{n}` 会在说话时替换成真实信息（等你的活儿数 / 会话总数）。
     * 这里不走 locale 服务：数量多、还带整句，直接按当前语言选一张表更省事。
     */
    const PHRASES = {
      zh: {
        sleep: ['呼……呼……（其实在假装工作）', '别叫我，我在跟周公对齐需求', '睡觉是最好的性能优化', '再睡五分钟就起来写代码', '梦里没有 bug'],
        eat: ['干饭时间到，谁也别拦我', '吃点西瓜，给 GPU 降降温', '甜甜圈和代码更配哦', '我不饿，我只是嘴馋', '先吃一口，需求稍后再说'],
        sing: ['啦啦啦～今天的编译一定过', '唱歌有益身心健康', '蹦迪使我快乐', '这首歌是写给没写完的测试的', '谁在听我唱，举个手'],
        slack: ['带薪拉屎，人生乐事', '我就摸一会儿鱼……哦我本来就是鱼', '摸鱼是对抗焦虑的良药', '老板路过记得叫我', '这个需求我先想想（其实在打游戏）'],
        daze: ['发呆中，请勿打扰', '在想一个很深的问题：今天吃什么', '大脑正在 GC', '你看我是不是有点圆', '什么都没想，脑子很干净'],
        mischief: ['嘿嘿，偷偷搞点事', '我把你的光标藏起来了（骗你的）', '皮一下很开心', '别拦我，我要开始整活了', '这个 bug 一定是别人的'],
        roam: ['到处看看有没有好吃的', '巡逻中，一切正常', '咦，这里怎么有个 TODO', '我溜达一圈就回来'],
        think: ['让我想想……这个循环为什么死不掉', '正在思考（其实在发呆）', '嗯……先看日志', '这个报错我见过（没见过）'],
        work: ['开始动手了，别催', '键盘都要敲冒烟了', '这个 bug 拆起来挺费劲', '干活中，请勿投喂', '让我把这个函数砍成三段'],
        attention: ['在等你点头呢', '有 {n} 个地方等你拍板', '你点一下我才能继续呀', '命令我，快', '审批按钮就在那边', '点点这里嘛，就一下', '我等你等到长蘑菇了'],
        done: ['刚干完一个，去看看？', '活儿交了，验收一下', '干完了！我要掌声', '那边收工了，别忘了', '验收一下嘛，我干得很认真的'],
        poke: ['干嘛呀，戳我', '哎呀呀，人家会害羞的啦', '再戳我就咬你哦，真的咬', '摸摸头也是可以的嘛', '你是不是没活干了', '呜哇，被戳到了', '你戳我一下，我开心一下'],
        hold: ['呜哇——放我下来！我怕高', '救命，我恐高啊', '你要把我扔哪去', '别晃了别晃了，晕', '我、我抓紧了！'],
        drop: ['呜……你摔我', '我就知道你不要我了', '好疼，要抱抱', '哼，我记住了', '你、你都不接住我'],
        pin: ['好，我就一直这样', '你说了算', '那我定格了', '喜欢这个是吧，那我一直摆着'],
        show: ['我回来了！想我没', '终于想起我了', '藏这么久都不找我', '重见天日'],
        switchPet: ['换成 {x} 了', '好，现在就由 {x} 值班', '{x} 上线！', '交接完毕：{x}'],
        imported: ['新伙伴 {x} 到岗了', '收到 {x} 这个新素材包', '解压好了，以后也能变成 {x}'],
        report: ['这次干了 {t}，写了 {o} tokens', '收工：{t} / {o} tokens，累死我了', '报告：耗时 {t}，输出 {o} tokens（总 {a}）', '干完啦，{t}、{o} tokens，值不值？'],
        reportTime: ['这次干了 {t}', '收工，耗时 {t}', '活儿交了，{t} 搞定'],
        // 干活期间的实时播报（按事件流）
        mThink: ['让我捋一捋…', '这个逻辑有点绕', '嗯……先看看现场', '有点眉目了', '唔…有点难'],
        mWrite: ['开始写结论了', '把思路整理一下', '这就写下来'],
        mRead: ['翻一下 {x}', '我看看这个文件', '在读 {x} 了'],
        mSearch: ['在找 {x}', '搜一下看看有几处', '这东西藏哪了…'],
        mEdit: ['动手改 {x}', '这行改一下就对了', '开始写代码了'],
        mRun: ['跑一下 {x}', '命令走你', '看看跑不跑得通'],
        mWeb: ['查一下资料', '我去网上看看', '搜搜有没有人踩过这个坑'],
        mPlan: ['先理一下步骤', '把清单更新一下', '计划是这样的…'],
        mLearn: ['翻翻技能手册', '让我看看有没有现成的招'],
        mDelegate: ['派个小弟去干', '这事分包出去', '叫人帮忙了'],
        mAsk: ['这个得问你', '我先确认一下再动手'],
        mPlugin: ['鼓捣一下插件', '在给运行时做手术'],
        mTool: ['用一下 {x}', '来点工具活'],
        mOk: ['过了', '嗯，符合预期', '这条通了', '不错不错', '嘿嘿，又过一关'],
        mFail: ['咦，报错了', '这条路堵了，换一条', '不对不对，重来', '失败也是一种信息'],
        mFailWhy: ['报错了：{r}', '工具回我：{r}', '嗯…{r}，我换个法子', '呜……{r}'],
        muse: ['今天有 {n} 个会话陪着我', '你写代码的样子真帅', '要不要休息一下？喝口水', '我盯你很久了', '这个项目什么时候上线呀'],
      },
      en: {
        sleep: ['Zzz… (pretending to work)', 'Dont wake me, I am aligning requirements with Morpheus', 'Sleep is the best performance optimization', 'Five more minutes, then I write code'],
        eat: ['Snack time, nobody stop me', 'Watermelon, to cool the GPU down', 'Donuts pair well with code', 'Not hungry, just greedy'],
        sing: ['La la la~ today it compiles', 'Singing is good for your health', 'Dancing makes me happy', 'This song is for the tests I never wrote'],
        slack: ['Paid bathroom break, a simple joy', 'Slacking off a bit… I am literally a fish', 'Slacking is an anti-anxiety drug', 'Tell me if the boss walks by'],
        daze: ['Spacing out, do not disturb', 'Thinking deep: what to eat today', 'Brain is running GC', 'Do I look a bit round?'],
        mischief: ['Hehe, causing a little trouble', 'I hid your cursor (just kidding)', 'Being naughty feels great', 'Step aside, mischief mode on'],
        roam: ['Looking around for snacks', 'Patrolling, all clear', 'Oh, a TODO over here', 'Just wandering nearby'],
        think: ['Let me think… why wont this loop die', 'Thinking hard (actually spacing out)', 'Hmm… lets read the logs first', 'I have seen this error before (I have not)'],
        work: ['Hands on now, dont rush me', 'The keyboard is smoking', 'This bug is stubborn', 'Working, do not feed me'],
        attention: ['Waiting for your nod', '{n} things need your call', 'Click it so I can continue', 'Command me, quick'],
        done: ['Just finished one, take a look?', 'Delivered, please review', 'Done! I want applause', 'That one wrapped up', 'Please review it, I worked hard'],
        poke: ['What? You poked me', 'Aw, I am shy', 'Poke me again and I bite', 'Head pats are fine too', 'Ouch, poked', 'Poke me once, happy once'],
        hold: ['Whoa — put me down! I fear heights', 'Help, I am scared of heights', 'Where are you taking me', 'Stop swinging, I am dizzy', 'I, I am holding on!'],
        drop: ['Ouch… you dropped me', 'I knew you did not want me', 'It hurts, hug me', 'Hmph, noted', 'You, you did not catch me'],
        pin: ['Fine, I will stay like this', 'Your call', 'Frozen then', 'You like this one, so I hold it'],
        show: ['I am back! Missed me?', 'Finally you remembered me', 'So long and you did not look', 'Back to daylight'],
        switchPet: ['Switched to {x}', 'Fine, {x} is on duty now', '{x} is online!', 'Handover done: {x}'],
        imported: ['New buddy {x} is on duty', 'Got the {x} pack', 'Extracted — {x} can join from now on'],
        report: ['That took {t} and {o} tokens', 'Done: {t} / {o} tokens, I am tired', 'Report: {t}, {o} output tokens ({a} total)', 'All done in {t} for {o} tokens'],
        reportTime: ['That took {t}', 'Done in {t}', 'Wrapped up in {t}'],
        // live play-by-play while working (driven by the session event stream)
        mThink: ['Let me untangle this', 'This logic is twisty', 'Hmm… let me look at the scene', 'Getting somewhere', 'Hmm… this one is hard'],
        mWrite: ['Writing the conclusion', 'Tidying up my thoughts', 'Putting it down now'],
        mRead: ['Checking {x}', 'Let me look at this file', 'Reading {x} now'],
        mSearch: ['Looking for {x}', 'Let me search around', 'Where is that thing hiding…'],
        mEdit: ['Editing {x}', 'One line and it is fixed', 'Writing code now'],
        mRun: ['Running {x}', 'Command, go', 'Let us see if it passes'],
        mWeb: ['Looking it up online', 'Checking the web', 'Someone must have hit this before'],
        mPlan: ['Let me plan the steps', 'Updating the list', 'Here is the plan…'],
        mLearn: ['Checking the skill book', 'Any existing trick for this?'],
        mDelegate: ['Sending a helper', 'Subcontracting this one', 'Calling for backup'],
        mAsk: ['This one is yours to answer', 'Let me confirm before acting'],
        mPlugin: ['Poking the plugin system', 'Surgery on the runtime'],
        mTool: ['Using {x}', 'Some tool work now'],
        mOk: ['It passed', 'As expected', 'That one worked', 'Nice', 'Hehe, another one down'],
        mFail: ['Uh oh, an error', 'Dead end, try another way', 'Nope, again', 'Failure is information too'],
        mFailWhy: ['It said: {r}', 'The tool told me: {r}', 'Hmm, {r} — trying another way', 'Aww… {r}'],
        muse: ['{n} sessions are keeping me company', 'You look good writing code', 'Take a break? Drink some water', 'I have been watching you'],
      },
    }
    const DEFAULT_PREFS = { x: 26, y: 104, size: 160, hidden: false, rate: 'calm', talk: true }
    const MIN_SIZE = 80
    const MAX_SIZE = 320

    /** 没事干的时候可以过的几种「生活」。 */
    const ACTIVITIES = [
      { id: 'sleep', pool: 'sleep', scale: 1.4 },
      { id: 'eat', pool: 'eat', scale: 1 },
      { id: 'sing', pool: 'sing', scale: 1 },
      { id: 'slack', pool: 'slack', scale: 1 },
      { id: 'daze', pool: 'daze', scale: 0.9 },
      { id: 'mischief', pool: 'mischief', scale: 0.8 },
      { id: 'roam', pool: 'roam', scale: 0.7 },
    ]

    /** 节奏档位：一段生活持续多久、每个动作待多久（毫秒）。 */
    const RATE_PRESETS = {
      calm: { activityMin: 90000, activityMax: 180000, ambientDwell: 10000, busyDwell: 6000, pokeDwell: 2800, celebrateDwell: 2600 },
      normal: { activityMin: 60000, activityMax: 120000, ambientDwell: 7000, busyDwell: 4800, pokeDwell: 2400, celebrateDwell: 2200 },
      lively: { activityMin: 30000, activityMax: 70000, ambientDwell: 5000, busyDwell: 3600, pokeDwell: 2000, celebrateDwell: 1800 },
    }
    const RATE_ORDER = ['calm', 'normal', 'lively']

    /**
     * 拖动和放下各自只锁定一个动作，全程不切换：
     * 拎起来时抽一个（一直播到松手），放下时再抽一个，播满它自己那一轮自然循环
     * 才结束 —— 所以不会停在动作播一半的地方。太短的动画至少停留 DROP_MIN_MS。
     */
    const DROP_MIN_MS = 1400

    /** 被拎住 / 刚被放下的样子（纯表现层，注入一次样式表）。 */
    const PET_CSS = `
@keyframes dsh-fish-pet-drop {
  0% { transform: translateY(-2px) scale(1.08, 0.92); }
  35% { transform: translateY(-12px) scale(0.95, 1.06); }
  70% { transform: translateY(0) scale(1.04, 0.97); }
  100% { transform: translateY(0) scale(1); }
}
@keyframes dsh-fish-pet-pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.3; transform: scale(0.8); }
}`

    /** 开工后先播多久「思考」再切到「干活」；0 就是整个任务只用一个动作。 */
    const THINK_LEAD_MS = 6000

    /**
     * 花名册：宠物 / 动画 / 池子。
     *
     * 默认是构建时写死的那份；组件挂载后会向 Host 要一份最新的（GET BASE/__roster），
     * 拿到就整体替换 —— 所以「往 assets/ 丢一只新宠物 → 面板里点重扫」或者
     * 「往 res/ 丢压缩包 → 面板里点导入」都能立刻用，不用重新构建客户端。
     * Host 半边不可用（没重启、老版本）时静默留在构建时的数据上，功能照旧。
     */
    const catalog = { pets: [], assets: [], pools: {}, source: 'built', generatedAt: null, warnings: [], res: null, scanMs: 0 }
    const BY_FILE = new Map()
    const ASSETS_BY_PET = new Map()
    const RESOLVED_POOLS = new Map()
    /** 素材全空时的占位动画：让渲染和状态机有个东西可以用，界面会改成提示卡片。 */
    const PLACEHOLDER = { f: '', u: '', n: '', ms: 2400, pet: '' }

    /** 用一份花名册整体替换当前数据（同时清掉按宠物缓存的池子）。 */
    function applyCatalog(next, source) {
      const data = next && typeof next === 'object' ? next : {}
      catalog.pets = Array.isArray(data.pets) ? data.pets.filter((pet) => pet && typeof pet.id === 'string') : []
      catalog.assets = Array.isArray(data.assets)
        ? data.assets.filter((asset) => asset && typeof asset.f === 'string' && typeof asset.u === 'string')
        : []
      catalog.pools = data.pools && typeof data.pools === 'object' ? data.pools : {}
      catalog.source = source
      catalog.generatedAt = typeof data.generatedAt === 'string' ? data.generatedAt : null
      catalog.warnings = Array.isArray(data.warnings) ? data.warnings : []
      catalog.res = data.res && typeof data.res === 'object' ? data.res : null
      catalog.scanMs = Number.isFinite(data.scanMs) ? data.scanMs : 0

      BY_FILE.clear()
      ASSETS_BY_PET.clear()
      RESOLVED_POOLS.clear()
      for (const asset of catalog.assets) {
        if (!BY_FILE.has(asset.f)) BY_FILE.set(asset.f, asset)
        if (!ASSETS_BY_PET.has(asset.pet)) ASSETS_BY_PET.set(asset.pet, [])
        ASSETS_BY_PET.get(asset.pet).push(asset)
      }
    }

    applyCatalog({ pets: BUILT_PETS, assets: BUILT_ASSETS, pools: BUILT_POOLS }, 'built')

    function petIds() {
      return catalog.pets.map((pet) => pet.id)
    }

    function defaultPet() {
      return catalog.pets.length > 0 ? catalog.pets[0].id : ''
    }

    function petAssets(petId) {
      return ASSETS_BY_PET.get(petId) || []
    }

    function petLabel(petId) {
      const pet = catalog.pets.find((entry) => entry.id === petId)
      return pet ? pet.label : petId
    }

    /** 某只宠物的某个池子 → 动画对象数组；池子没配或名字对不上就退回该宠物的全部素材。 */
    function pool(petId, key) {
      const cacheKey = `${petId}|${key}`
      if (RESOLVED_POOLS.has(cacheKey)) return RESOLVED_POOLS.get(cacheKey)
      const petPools = catalog.pools[petId] || {}
      const names = petPools[key]
      const list = Array.isArray(names) ? names.map((file) => BY_FILE.get(file)).filter(Boolean) : []
      const result = list.length > 0 ? list : petAssets(petId)
      RESOLVED_POOLS.set(cacheKey, result)
      return result
    }

    const DICT = {
      'zh-CN': {
        label: '蓝色大肥鱼桌宠',
        title: '蓝色大肥鱼桌宠',
        hint: '拖动移动 · 单击逗它 · 双击或右键打开面板',
        size: '大小',
        rate: '换动作',
        talk: '说话',
        talkOn: '开',
        talkOff: '关',
        sayOne: '说一句',
        calm: '安静',
        normal: '正常',
        lively: '活泼',
        poke: '逗一下',
        auto: '恢复自动',
        hide: '藏起来',
        show: '把鱼放出来',
        reset: '复位位置',
        search: '搜索动画…',
        noMatch: '没有匹配的动画',
        noAssets: '没有桌宠素材',
        loadFail: '桌宠素材加载失败',
        buildHint: '先跑 node tools/build-client.mjs',
        total: '个动画可选',
        playing: '正在播',
        state: 'DSH',
        working: '干活中',
        waiting: '等你回答',
        attention: '等你回答',
        running: '正在干活',
        done: '刚干完活',
        idle: '没事干',
        pet: '宠物',
        pets: '桌宠',
        petSwap: '换一只',
        sleep: '睡觉',
        eat: '干饭',
        sing: '唱歌蹦迪',
        slack: '摸鱼',
        daze: '发呆',
        mischief: '搞事',
        roam: '瞎逛',
        pin: '你选的',
        hold: '被拎着',
        drop: '刚被放下',
        pokeState: '卖萌',
        celebrate: '庆祝',
        diag: '诊断',
        srcSlot: '槽 props',
        srcService: '会话服务',
        srcNone: '都不可用',
        sessions: '会话',
        sRun: '干活',
        sWait: '等待',
        sDone: '未读',
        sTokens: 'token',
        sEvents: '事件',
        rescan: '重扫',
        rescanTitle: '重新扫描 assets/ 目录（丢进去的新宠物目录立刻出现）',
        import: '导入',
        importTitle: '把 res/ 里的压缩包解压成新宠物',
        importForce: '强制重解压',
        importForceTitle: '忽略时间戳，把 res/ 里的压缩包全部重新解压一遍',
        importing: '解压中…',
        importNone: 'res/ 里没有待导入的压缩包',
        importFail: '导入失败',
        roster: '花名册',
        rosterRuntime: '运行时',
        rosterBuilt: '构建时',
        rosterFail: '拉花名册失败（Host 半边可能要重启 dsh）',
        petHint: '换宠物：直接把桌宠压缩包丢进 res/ 点「导入」，或往 assets/ 放一个目录点「重扫」',
        noAssetsHint: '把桌宠压缩包（.7z / .zip）丢进 res/，然后点下面的「导入」',
        archive: '压缩包',
      },
      en: {
        label: 'Blue Fish desktop pet',
        title: 'Blue Fish pet',
        hint: 'drag to move · click to poke · double-click or right-click for the panel',
        size: 'Size',
        rate: 'Pace',
        talk: 'Talk',
        talkOn: 'on',
        talkOff: 'off',
        sayOne: 'Say something',
        calm: 'Calm',
        normal: 'Normal',
        lively: 'Lively',
        poke: 'Poke',
        auto: 'Auto mode',
        hide: 'Hide',
        show: 'Bring the fish back',
        reset: 'Reset position',
        search: 'Search animations…',
        noMatch: 'no matching animation',
        noAssets: 'No pet assets',
        loadFail: 'Pet assets failed to load',
        buildHint: 'run node tools/build-client.mjs first',
        total: 'animations',
        playing: 'Now',
        state: 'DSH',
        working: 'working',
        waiting: 'waiting for you',
        attention: 'waiting for you',
        running: 'working',
        done: 'just finished',
        idle: 'idle',
        pet: 'Pet',
        pets: 'Pets',
        petSwap: 'Switch pet',
        sleep: 'sleeping',
        eat: 'eating',
        sing: 'singing',
        slack: 'slacking',
        daze: 'daydreaming',
        mischief: 'causing trouble',
        roam: 'wandering',
        pin: 'pinned',
        hold: 'held up',
        drop: 'just dropped',
        pokeState: 'being cute',
        celebrate: 'celebrating',
        diag: 'diag',
        srcSlot: 'slot props',
        srcService: 'session service',
        srcNone: 'unavailable',
        sessions: 'sessions',
        sRun: 'running',
        sWait: 'waiting',
        sDone: 'unread',
        sTokens: 'tokens',
        sEvents: 'events',
        rescan: 'Rescan',
        rescanTitle: 'Rescan the assets/ directory (a new pet folder shows up immediately)',
        import: 'Import',
        importTitle: 'Extract the archives in res/ as new pets',
        importForce: 'Force',
        importForceTitle: 'Ignore timestamps and re-extract every archive in res/',
        importing: 'Extracting…',
        importNone: 'nothing new to import in res/',
        importFail: 'import failed',
        roster: 'Roster',
        rosterRuntime: 'runtime',
        rosterBuilt: 'built-in',
        rosterFail: 'roster fetch failed (the host half may need a dsh restart)',
        petHint: 'Switch pets: drop a pack into res/ and hit Import, or drop a folder into assets/ and hit Rescan',
        noAssetsHint: 'Drop a pet pack (.7z / .zip) into res/ and hit Import below',
        archive: 'archives',
      },
    }

    function clamp(value, min, max) {
      return Math.min(Math.max(value, min), max)
    }

    /** 把秒数说成人话：83 → 「1 分 23 秒」。 */
    function formatDuration(seconds) {
      const total = Math.max(1, Math.round(seconds))
      if (total < 60) return `${total} 秒`
      const minutes = Math.floor(total / 60)
      const rest = total % 60
      return rest === 0 ? `${minutes} 分钟` : `${minutes} 分 ${rest} 秒`
    }

    /** 1234 → 「1.2k」。 */
    function formatTokens(count) {
      if (count < 1000) return String(Math.max(0, Math.round(count)))
      return `${(count / 1000).toFixed(1)}k`
    }

    /**
     * 会话列表里宿主算好的 token 投影：把每个会话的累计用量加起来。
     * 返回数字（选择器 hook 靠值比较决定要不要重渲染，数字最稳）。
     */
    function selectTokenTotal(state) {
      let total = 0
      if (state && state.byId) {
        for (const id of Object.keys(state.byId)) {
          const usage = state.byId[id] && state.byId[id].projectionValues && state.byId[id].projectionValues.tokenUsage
          if (usage) total += (usage.uncachedInputTokens || 0) + (usage.outputTokens || 0)
        }
      }
      return total
    }

    function selectTokenOutput(state) {
      let total = 0
      if (state && state.byId) {
        for (const id of Object.keys(state.byId)) {
          const usage = state.byId[id] && state.byId[id].projectionValues && state.byId[id].projectionValues.tokenUsage
          if (usage) total += usage.outputTokens || 0
        }
      }
      return total
    }

    /** 槽里没给 useSessions 时的退路。 */
    function useNoSessions() {
      return 0
    }

    /**
     * 该盯哪个会话：优先正在干活的那个，其次主视图里那个。
     * 返回会话 id 字符串（选择器 hook 靠值比较，字符串最稳）。
     */
    function selectWatchedSession(state) {
      if (!state || !state.byId) return ''
      const ids = Array.isArray(state.ids) && state.ids.length > 0 ? state.ids : Object.keys(state.byId)
      let running = ''
      let main = ''
      for (const id of ids) {
        const row = state.byId[id]
        if (!row) continue
        if (running === '' && row.running === true) running = id
        if (main === '' && row.retainedBy && row.retainedBy.mainView > 0) main = id
      }
      return running || main || ''
    }

    /** 干活播报的最小间隔：太密就像解说员了。 */
    const WORK_SAY_GAP_MS = 15000

    /** 颜文字：按情绪分组，说话时随机挂一个（不用逐句手写）。 */
    const KAOMOJI = {
      happy: ['(◕‿◕)', '(*≧▽≦)', '(๑•̀ㅂ•́)و✧', 'ヾ(≧▽≦*)o', '(๑˃̵ᴗ˂̵)', '(◍•ᴗ•◍)'],
      shy: ['(///▽///)', '(๑´ω`๑)', '(⁄ ⁄•⁄ω⁄•⁄)', '(〃∀〃)'],
      sad: ['(｡•́︿•̀｡)', '(っ˘̩╭╮˘̩)っ', '(｡╯︵╰｡)'],
      panic: ['(°ロ°)', 'Σ(°△°|||)', '(ﾟДﾟ;)', '(((ºДº;)'],
      sleepy: ['(¦3[▓▓]', '(っ˘ω˘ς)', '(－_－) zzZ'],
      think: ['(・∀・)', '(￣▽￣)', '(。・ω・。)', '(๑･ω･๑)'],
      work: ['(๑•̀ㅂ•́)و', '(ง •̀_•́)ง', '(๑•̀ㅁ•́๑)✧'],
      plead: ['(´･ω･`)', '(｡･ω･｡)ﾉ♡', '(๑•́ ₃ •̀๑)'],
      proud: ['(๑˃ᴗ˂)ﻭ', 'ᕦ(ò_óˇ)ᕤ', '(•̀ᴗ•́)و'],
    }

    /** 每个台词池对应哪种情绪。没列的池就不挂颜文字。 */
    const POOL_MOOD = {
      poke: 'shy',
      moe: 'shy',
      show: 'happy',
      celebrate: 'happy',
      pin: 'shy',
      hold: 'panic',
      drop: 'sad',
      mFail: 'sad',
      mFailWhy: 'sad',
      sleep: 'sleepy',
      eat: 'happy',
      sing: 'happy',
      slack: 'shy',
      daze: 'sleepy',
      mischief: 'happy',
      roam: 'think',
      think: 'think',
      mThink: 'think',
      mWeb: 'think',
      mLearn: 'think',
      muse: 'think',
      work: 'work',
      mWrite: 'work',
      mRead: 'work',
      mSearch: 'work',
      mEdit: 'work',
      mRun: 'work',
      mPlan: 'work',
      mPlugin: 'work',
      mTool: 'work',
      mDelegate: 'proud',
      mOk: 'proud',
      report: 'proud',
      reportTime: 'proud',
      mAsk: 'plead',
      attention: 'plead',
      done: 'happy',
      notify: 'happy',
    }

    /** 挂颜文字的概率；剩下的时候留白，免得每句都花里胡哨。 */
    const KAOMOJI_CHANCE = 0.75

    /** 情绪 → 气泡描边颜色（和颜文字同一个情绪，深色主题下也压得住）。 */
    const MOOD_TINT = {
      happy: '#ff8fb1',
      shy: '#ff9ec4',
      sad: '#8fb8ff',
      panic: '#ffb27a',
      sleepy: '#a99cff',
      think: '#84d2ff',
      work: '#8fdca0',
      plead: '#ffc46b',
      proud: '#ffd166',
    }

    /** 给台词挂一个对味的颜文字；句子本来就以括号结尾（或已有颜文字）就不叠。 */
    function decorate(text, poolKey) {
      const mood = POOL_MOOD[poolKey]
      const list = mood ? KAOMOJI[mood] : null
      if (!Array.isArray(list) || list.length === 0) return text
      if (Math.random() > KAOMOJI_CHANCE) return text
      if (/[)）]$/.test(text)) return text
      return `${text} ${list[Math.floor(Math.random() * list.length)]}`
    }

    /** 工具名 → 播报分类（池名后缀）。 */
    const TOOL_CATEGORY = {
      read: 'Read',
      read_image: 'Read',
      grep: 'Search',
      glob: 'Search',
      edit: 'Edit',
      write: 'Edit',
      present: 'Edit',
      todo_write: 'Plan',
      create_goal: 'Plan',
      get_goal: 'Plan',
      update_goal: 'Plan',
      bash: 'Run',
      pwsh: 'Run',
      job_list: 'Run',
      job_output: 'Run',
      job_kill: 'Run',
      web_search: 'Web',
      web_fetch: 'Web',
      skill: 'Learn',
      subagent: 'Delegate',
      subagent_fork: 'Delegate',
      workflow: 'Delegate',
      ask_user_question: 'Ask',
      cordis_define: 'Plugin',
      cordis_run: 'Plugin',
      cordis_stop: 'Plugin',
      cordis_undefine: 'Plugin',
      cordis_inspect_list: 'Plugin',
      cordis_inspect_query: 'Plugin',
    }

    /** 从工具参数里抠一个能说出口的细节（文件名、命令、搜索词…）。 */
    function toolDetail(argumentsJson) {
      let args = null
      try {
        args = JSON.parse(String(argumentsJson || '{}'))
      } catch {
        args = null
      }
      if (!args || typeof args !== 'object') return ''
      const text = (key) => (typeof args[key] === 'string' ? args[key] : '')
      const base = (value) => {
        const parts = String(value).split(/[\\/]/)
        return parts[parts.length - 1] || ''
      }
      const first = (value, words) => String(value).split(/\s+/).slice(0, words).join(' ').slice(0, 24)
      if (text('file_path') !== '') return base(text('file_path'))
      if (text('path') !== '') return base(text('path'))
      if (text('pattern') !== '') return text('pattern').slice(0, 28)
      if (text('command') !== '') return first(text('command'), 2)
      if (text('query') !== '') return text('query').slice(0, 24)
      if (text('url') !== '') return text('url').slice(0, 24)
      if (text('description') !== '') return text('description').slice(0, 24)
      return ''
    }

    /**
     * 把事件流最新的一条翻成「一个瞬间」：她在想 / 在写 / 叫了哪个工具 / 工具成了还是炸了。
     * 返回 { pool, vars } —— pool 是台词池名，vars 是替换进台词里的真实细节。
     * @param entry 事件窗口里的一条（durable 或 transient）
     * @param calls callId → { name, detail } 的记忆表（tool/result 本身不带工具名）
     */
    function momentOf(entry, calls) {
      if (!entry || typeof entry !== 'object') return null
      const event = entry.event
      if (!event || typeof event !== 'object') return null

      // 实时流：思考 / 写答案 / 准备调工具
      if (entry.type === 'transient') {
        const chunk = event.data && event.data.chunk
        if (!chunk || typeof chunk !== 'object') return null
        if (chunk.type === 'reasoning-delta' && typeof chunk.text === 'string' && chunk.text.trim() !== '') {
          return { pool: 'mThink', vars: {} }
        }
        if (chunk.type === 'text-delta' && typeof chunk.text === 'string' && chunk.text.trim() !== '') {
          return { pool: 'mWrite', vars: {} }
        }
        return null
      }

      if (event.type === 'tool/call') {
        const data = event.data || {}
        const name = typeof data.name === 'string' ? data.name : 'tool'
        const detail = toolDetail(data.arguments)
        if (typeof data.callId === 'string') {
          calls.set(data.callId, { name, detail })
          // 长时间会话别把这张表攒爆：只留最近的记录。
          if (calls.size > 400) {
            const oldest = calls.keys().next().value
            if (oldest !== undefined) calls.delete(oldest)
          }
        }
        return { pool: `m${TOOL_CATEGORY[name] || 'Tool'}`, vars: { x: detail || name } }
      }

      if (event.type === 'tool/result') {
        const data = event.data || {}
        const block = data.message && Array.isArray(data.message.content) ? data.message.content[0] : null
        const callId = block && typeof block.toolCallId === 'string' ? block.toolCallId : ''
        const remembered = callId !== '' ? calls.get(callId) : undefined
        const failed = data.error !== undefined || (block && block.isError === true)
        const reason = data.error && typeof data.error.reason === 'string' ? data.error.reason.slice(0, 40) : ''
        return {
          // 有真实错误原因就直接报出来，比笼统的「报错了」有意思。
          pool: failed ? (reason === '' ? 'mFail' : 'mFailWhy') : 'mOk',
          vars: { x: (remembered && (remembered.detail || remembered.name)) || '', r: reason },
        }
      }

      if (event.type === 'assistant/message') {
        const data = event.data || {}
        if (data.interrupted === true) return { pool: 'mFail', vars: { x: '', r: '' } }
        return null
      }

      return null
    }

    /**
     * 桌宠是常驻的悬浮层，一旦渲染抛错会把整个 AppFrame 一起带走，
     * 所以自己兜住错误：坏掉的只是这条鱼，不是用户的界面。
     */
    class PetBoundary extends React.Component {
      constructor(props) {
        super(props)
        this.state = { failed: false }
      }

      static getDerivedStateFromError() {
        return { failed: true }
      }

      componentDidCatch(error) {
        console.error('dsh-fish-pet: render failed, desktop pet disabled for this session', error)
      }

      render() {
        return this.state.failed ? null : this.props.children
      }
    }

    function loadPrefs() {
      const base = { ...DEFAULT_PREFS, pet: defaultPet() }
      try {
        const raw = window.localStorage.getItem(STORAGE_KEY)
        if (!raw) return base
        const parsed = JSON.parse(raw)
        return {
          ...base,
          x: Number.isFinite(parsed.x) ? parsed.x : base.x,
          y: Number.isFinite(parsed.y) ? parsed.y : base.y,
          size: Number.isFinite(parsed.size) ? clamp(parsed.size, MIN_SIZE, MAX_SIZE) : base.size,
          hidden: parsed.hidden === true,
          rate: RATE_ORDER.includes(parsed.rate) ? parsed.rate : base.rate,
          talk: parsed.talk !== false,
          // 素材换过之后旧 id 可能不存在了，退回默认宠物（花名册到位后还会再校一次）。
          pet: petIds().includes(parsed.pet) ? parsed.pet : base.pet,
        }
      } catch {
        return base
      }
    }

    function savePrefs(prefs) {
      try {
        window.localStorage.setItem(STORAGE_KEY, JSON.stringify(prefs))
      } catch {
        /* 隐私模式下写不进去就算了，本次会话仍然可用 */
      }
    }

    function srcOf(asset) {
      // 用 build 阶段算好的 URL 片段（文件名）：Host 半边按文件名在整棵素材树里找，
      // 所以素材挪进子目录、换压缩包都不影响这里。
      return `${BASE}/${asset.u}`
    }

    function pick(list, avoid) {
      // 素材整个空的时候也要给渲染一个东西用（界面会换成提示卡片）。
      const poolList = list && list.length > 0 ? list : catalog.assets
      if (poolList.length === 0) return PLACEHOLDER
      if (poolList.length === 1) return poolList[0]
      for (let attempt = 0; attempt < 8; attempt += 1) {
        const candidate = poolList[Math.floor(Math.random() * poolList.length)]
        if (!avoid || candidate.f !== avoid.f) return candidate
      }
      return poolList[0]
    }

    /**
     * 把整张会话状态表压成一个字符串：`阶段|会话数|干活|等待|未读`。
     * 返回字符串而不是对象，是因为选择器 hook 靠值比较决定要不要重渲染。
     */
    function worldSummaryOf(statuses) {
      let total = 0
      let running = 0
      let waiting = 0
      let finished = 0
      if (statuses && typeof statuses.forEach === 'function') {
        statuses.forEach((status) => {
          total += 1
          if (!status) return
          if (status.pendingInteraction) waiting += 1
          if (status.running === true) running += 1
          if (status.completionUnread === true) finished += 1
        })
      }
      const phase = waiting > 0 ? 'attention' : running > 0 ? 'running' : finished > 0 ? 'done' : 'idle'
      return `${phase}|${total}|${running}|${waiting}|${finished}`
    }

    function parseWorldSummary(summary) {
      const parts = typeof summary === 'string' ? summary.split('|') : []
      return {
        phase: parts[0] || 'idle',
        total: Number(parts[1] ?? 0),
        running: Number(parts[2] ?? 0),
        waiting: Number(parts[3] ?? 0),
        finished: Number(parts[4] ?? 0),
      }
    }

    /** 槽里没给 useSessionStatus 时的退路：世界永远「没事干」，宠物照样活着。 */
    function useNoWorldSummary() {
      return 'idle|0|0|0|0'
    }

    /** 槽的标准 props 路线（正规做法：shell.overlay 会传 useSessionStatus）。 */
    function useSlotWorldSummary(hook) {
      const useHook = typeof hook === 'function' ? hook : useNoWorldSummary
      return useHook(worldSummaryOf)
    }

    /**
     * 服务路线：直接订阅 ctx.uiSession.sessionStatus。
     * 万一槽没给 hook（或者注册组件忘了转发 props）时兜底。
     */
    function useServiceWorldSummary(store) {
      const read = React.useCallback(() => (store ? store.read() : 'idle|0|0|0|0'), [store])
      const [summary, setSummary] = React.useState(read)
      React.useEffect(() => {
        if (!store) return undefined
        setSummary(store.read())
        return store.subscribe(() => setSummary(store.read()))
      }, [store, read])
      return summary
    }

    /** 素材缺失/加载失败时的提示条样式。 */
    const NOTICE_STYLE = {
      position: 'fixed',
      right: 14,
      bottom: 104,
      zIndex: 62,
      maxWidth: 260,
      pointerEvents: 'auto',
      padding: '6px 10px',
      borderRadius: 10,
      border: '1px solid var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
      background: 'var(--dsw-alias-bg-overlay, rgba(255,255,255,0.97))',
      color: 'var(--dsw-alias-label-primary, inherit)',
      fontSize: 11,
      lineHeight: 1.5,
      boxShadow: '0 6px 18px rgba(0,0,0,0.18)',
    }

    function FishPet(props) {
      const t = props.t
      // t 放进 ref：花名册刷新那几个 useCallback 要保持稳定，
      // 不能因为每次渲染的 t 换了身份就重建（否则挂载时的刷新 effect 会自己转起来）。
      const tRef = React.useRef(t)
      tRef.current = t
      // 诊断用的词典状态（服务没了/没绑上时，面板的 i 行会显出来）。
      const localeInfo = typeof props.localeInfo === 'function' ? props.localeInfo : () => 'n/a'

      const timer = props.timer
      // 两条路都订阅（hook 顺序固定），优先用槽给的标准 props。
      const slotSummary = useSlotWorldSummary(props.useSessionStatus)
      const serviceSummary = useServiceWorldSummary(props.statusStore)
      const summary = typeof props.useSessionStatus === 'function' ? slotSummary : serviceSummary
      const world = parseWorldSummary(summary)
      const worldPhase = world.phase
      const sourceKey =
        typeof props.useSessionStatus === 'function' ? 'srcSlot' : props.statusStore ? 'srcService' : 'srcNone'
      // 本次干活的 token：宿主投影是「会话累计」，所以收工时算差值。
      const useSessionsSafe = typeof props.useSessions === 'function' ? props.useSessions : useNoSessions
      const tokenTotal = useSessionsSafe(selectTokenTotal)
      const tokenOutput = useSessionsSafe(selectTokenOutput)
      const watchedSessionId = useSessionsSafe(selectWatchedSession)

      const [prefs, setPrefs] = React.useState(loadPrefs)
      const [asset, setAsset] = React.useState(() => pick(pool(prefs.pet, 'daze')))
      const [token, setToken] = React.useState(0)
      const [pinned, setPinned] = React.useState(null)
      const [signal, setSignal] = React.useState(0)
      const [activity, setActivity] = React.useState(null)
      const [machineState, setMachineState] = React.useState('idle')
      const [panelOpen, setPanelOpen] = React.useState(false)
      const [query, setQuery] = React.useState('')
      const [dragging, setDragging] = React.useState(false)
      const [showDiag, setShowDiag] = React.useState(false)
      const [bubble, setBubble] = React.useState(null)
      // 图片连续加载失败（素材被删了、assetDir 指错了）也要说出来，而不是默默显示裂图。
      const [imageFailures, setImageFailures] = React.useState(0)
      const [noticeHidden, setNoticeHidden] = React.useState(false)
      // 花名册：source 决定「运行时」还是「构建时」，导入/重扫的结果用 note 说一句。
      const [rosterSeq, setRosterSeq] = React.useState(0)
      const [rosterState, setRosterState] = React.useState('idle')
      const [importing, setImporting] = React.useState(false)
      const [rosterNote, setRosterNote] = React.useState(null)

      const prefsRef = React.useRef(prefs)
      const assetRef = React.useRef(asset)
      const pinnedRef = React.useRef(pinned)
      const phaseRef = React.useRef(worldPhase)
      const draggingRef = React.useRef(dragging)
      const machineRef = React.useRef({
        pokeUntil: 0,
        celebrateUntil: 0,
        dropUntil: 0,
        holdAsset: null,
        dropAsset: null,
        runningSince: Date.now(),
        runAsset: null,
        runPhase: null,
        usageAtStart: null,
        doneSince: 0,
        activity: null,
        activityUntil: 0,
        activitySay: null,
      })
      const previousPhaseRef = React.useRef(worldPhase)
      const worldRef = React.useRef(world)
      const tokenRef = React.useRef({ total: tokenTotal, output: tokenOutput })
      const reportTimerRef = React.useRef(null)
      const sayRef = React.useRef(null)
      const lastSpokeAtRef = React.useRef(0)
      const lastLineRef = React.useRef({})
      const bubbleSeqRef = React.useRef(0)
      prefsRef.current = prefs
      assetRef.current = asset
      pinnedRef.current = pinned
      phaseRef.current = worldPhase
      draggingRef.current = dragging
      worldRef.current = world
      tokenRef.current = { total: tokenTotal, output: tokenOutput }

      const update = React.useCallback((patch) => {
        setPrefs((current) => {
          const next = { ...current, ...patch }
          savePrefs(next)
          return next
        })
      }, [])

      /** 当前语言的台词表（说话时现读，切换语言立刻生效）。 */
      const phraseTable = React.useCallback(() => {
        const active = typeof props.activeLocale === 'function' ? props.activeLocale() : 'zh'
        return /^zh/i.test(String(active)) ? PHRASES.zh : PHRASES.en
      }, [props.activeLocale])

      /** 定时一次，优先用 cordis timer（跟着插件 fiber 自动清理）。 */
      const scheduleOnce = React.useCallback(
        (fn, ms) => {
          if (timer && typeof timer.timeout === 'function') return timer.timeout(fn, ms)
          const id = window.setTimeout(fn, ms)
          return () => window.clearTimeout(id)
        },
        [timer],
      )

      /**
       * 说一句话。force 用于用户交互（点它、拖它、放下它、点列表），
       * 不受冷却限制；状态机自己触发的台词有 9 秒冷却，免得一直叨叨。
       */
      const speak = React.useCallback(
        (poolKey, options) => {
          const opts = options || {}
          if (prefsRef.current.talk === false) return
          // reveal：刚好在「放出来」的那一刻说话，此时 hidden 还是 true，得放行。
          if (prefsRef.current.hidden && opts.reveal !== true) return
          const table = phraseTable() || PHRASES.zh
          const lines = table[poolKey] || PHRASES.zh[poolKey]
          if (!Array.isArray(lines) || lines.length === 0) return
          const now = Date.now()
          if (opts.force !== true && now - lastSpokeAtRef.current < 9000) return
          let index = Math.floor(Math.random() * lines.length)
          if (lines.length > 1 && index === lastLineRef.current[poolKey]) index = (index + 1) % lines.length
          lastLineRef.current[poolKey] = index
          const vars = Object.assign({ n: opts.n === undefined ? '' : opts.n }, opts.vars || {})
          let text = String(lines[index])
          for (const key of Object.keys(vars)) text = text.split(`{${key}}`).join(String(vars[key]))
          text = decorate(text, poolKey)
          lastSpokeAtRef.current = now
          bubbleSeqRef.current += 1
          setBubble({
            id: bubbleSeqRef.current,
            text,
            mood: POOL_MOOD[poolKey] || null,
            until: now + clamp(2000 + text.length * 110, 2600, 9000),
          })
        },
        [phraseTable],
      )

      // 说明说完就收起来；台词越长挂得越久。
      React.useEffect(() => {
        if (bubble === null) return undefined
        const hold = Math.max(1200, bubble.until - Date.now())
        return scheduleOnce(() => setBubble(null), hold)
      }, [bubble, scheduleOnce])

      // ── 花名册：运行时从 Host 拉一份最新的宠物/动画/池子 ────────────────────
      /** 清掉状态机里的临时状态（换宠物 / 花名册变了之后不能让旧池子的动作留着）。 */
      const resetMachine = React.useCallback(() => {
        const machine = machineRef.current
        machine.pokeUntil = 0
        machine.celebrateUntil = 0
        machine.dropUntil = 0
        machine.holdAsset = null
        machine.dropAsset = null
        machine.runAsset = null
        machine.runPhase = null
        machine.activity = null
        machine.activityUntil = 0
        machine.activitySay = null
        setPinned(null)
      }, [])

      /**
       * 换一只宠物：纯前端切换，立刻换图（不等状态机下一个节拍），用新宠物的池子继续过日子。
       */
      const switchPet = React.useCallback(
        (petId) => {
          if (petId === prefsRef.current.pet) return
          if (!petIds().includes(petId)) return
          resetMachine()
          update({ pet: petId })
          const next = pick(pool(petId, 'daze'))
          if (next.f !== assetRef.current.f) setToken((n) => n + 1)
          assetRef.current = next
          setAsset(next)
          setSignal((n) => n + 1)
          speak('switchPet', { force: true, vars: { x: petLabel(petId) } })
        },
        [resetMachine, speak, update],
      )

      /**
       * 拉一次花名册。拿到就整体替换掉构建时写死的数据；失败就静默留在原数据上
       * （Host 半边没重启时就是这种情况，不该弹一堆错）。
       */
      const refreshRoster = React.useCallback(async (options) => {
        const opts = options || {}
        setRosterState('loading')
        try {
          const response = await fetch(`${BASE}/__roster${opts.force === true ? '?refresh=1' : ''}`, {
            headers: { accept: 'application/json' },
            cache: 'no-store',
          })
          if (!response.ok) throw new Error(`HTTP ${response.status}`)
          const data = await response.json()
          applyCatalog(data, 'runtime')
          setRosterState('ok')
          setRosterSeq((n) => n + 1)
          setRosterNote(null)
          return data
        } catch (error) {
          setRosterState('error')
          if (opts.loud === true) {
            setRosterNote(`${tRef.current('rosterFail')}（${error && error.message ? error.message : String(error)}）`)
          }
          return null
        }
      }, [])

      /** 把 res/ 里的压缩包解压成新宠物（面板上的「导入」）。 */
      const importFromRes = React.useCallback(
        async (force) => {
          setImporting(true)
          setRosterNote(null)
          try {
            const response = await fetch(`${BASE}/__import`, {
              method: 'POST',
              headers: { 'content-type': 'application/json', accept: 'application/json', 'x-fish-pet': 'import' },
              body: JSON.stringify({ force: force === true }),
            })
            const data = await response.json().catch(() => null)
            if (!response.ok) throw new Error(data && data.error ? data.error : `HTTP ${response.status}`)
            if (data && Array.isArray(data.pets)) {
              applyCatalog(data, 'runtime')
              setRosterState('ok')
              setRosterSeq((n) => n + 1)
            }
            const imported = data && Array.isArray(data.imported) ? data.imported : []
            const failures = data && Array.isArray(data.failures) ? data.failures : []
            if (imported.length > 0) {
              const names = imported.map((id) => petLabel(id)).join('、')
              speak('imported', { force: true, vars: { x: names } })
              setRosterNote(`${tRef.current('import')}：${names}`)
              // 只导入了一只就顺手切过去，省掉一次点击。
              if (imported.length === 1) switchPet(imported[0])
            } else if (failures.length > 0) {
              setRosterNote(`${tRef.current('importFail')}：${failures[0].archive} — ${failures[0].reason}`)
            } else {
              setRosterNote(tRef.current('importNone'))
            }
            return data
          } catch (error) {
            setRosterNote(`${tRef.current('importFail')}：${error && error.message ? error.message : String(error)}`)
            return null
          } finally {
            setImporting(false)
          }
        },
        [speak, switchPet],
      )

      // 挂载后拉一次花名册（Host 半边是权威数据源，构建时那份只是兜底）。
      React.useEffect(() => {
        void refreshRoster({})
      }, [refreshRoster])

      // 花名册换了之后，选中的宠物可能已经不在里面（素材被删/换包）→ 退回第一只。
      React.useEffect(() => {
        const ids = petIds()
        if (ids.length === 0) return
        if (ids.includes(prefsRef.current.pet)) return
        resetMachine()
        const next = pick(pool(ids[0], 'daze'))
        if (next.f !== assetRef.current.f) setToken((n) => n + 1)
        assetRef.current = next
        setAsset(next)
        update({ pet: ids[0] })
        setSignal((n) => n + 1)
      }, [rosterSeq, resetMachine, update])

      // ── 干活播报：订阅当前会话的事件流，看她在想什么、调了什么工具、成没成 ──
      const binding = React.useMemo(() => {
        const service = props.sessionsService
        if (!service || watchedSessionId === '' || typeof service.binding !== 'function') return null
        try {
          return service.binding(watchedSessionId) || null
        } catch {
          return null
        }
      }, [props.sessionsService, watchedSessionId])

      const callsRef = React.useRef(new Map())
      const lastWorkSayRef = React.useRef(0)
      const lastMomentRef = React.useRef('')
      const eventCountRef = React.useRef(-1)

      const announce = React.useCallback(
        (moment) => {
          const now = Date.now()
          if (now - lastWorkSayRef.current < WORK_SAY_GAP_MS) return
          if (Math.random() < 0.3) return // 不是每个瞬间都播报，不然像解说员
          lastWorkSayRef.current = now
          speak(moment.pool, { force: true, vars: moment.vars })
        },
        [speak],
      )

      React.useEffect(() => {
        const source = binding && binding.eventSource
        if (!source || typeof source.subscribe !== 'function') return undefined
        const onPublish = () => {
          if (phaseRef.current !== 'running') return
          const snapshot = source.getSnapshot()
          const entries = snapshot && snapshot.entries
          if (!Array.isArray(entries)) return
          eventCountRef.current = entries.length
          if (entries.length === 0) return
          const key = String(snapshot.revision)
          if (key === lastMomentRef.current) return
          lastMomentRef.current = key
          const moment = momentOf(entries[entries.length - 1], callsRef.current)
          if (moment) announce(moment)
        }
        onPublish()
        return source.subscribe(onPublish)
      }, [binding, announce])

      // 干活时常驻的状态条要显示「干了多久」，所以干活期间每秒重渲染一次。
      const [beat, setBeat] = React.useState(0)
      React.useEffect(() => {
        if (worldPhase !== 'running') return undefined
        const bump = () => setBeat((n) => n + 1)
        if (timer && typeof timer.interval === 'function') return timer.interval(bump, 1000)
        const id = window.setInterval(bump, 1000)
        return () => window.clearInterval(id)
      }, [worldPhase, timer])

      const poke = React.useCallback(() => {
        machineRef.current.pokeUntil = Date.now() + 4200
        setSignal((n) => n + 1)
        speak('poke', { force: true })
      }, [speak])

      /** 下一步该播什么、多久之后再算下一步。 */
      const plan = React.useCallback(() => {
        const now = Date.now()
        const machine = machineRef.current
        const preset = RATE_PRESETS[prefsRef.current.rate] ?? RATE_PRESETS.calm
        // 当前这只宠物：池子是按宠物分的，所以每个 pool() 都要带上它。
        const pet = prefsRef.current.pet

        // 点它永远优先：即使固定了某个动画、或刚被放下，也要有可见反应，
        // 反应完再回到本该播的那个（否则「点它没反应」会变成 bug）。
        if (now < machine.pokeUntil) {
          return { asset: pick(pool(pet, 'moe'), assetRef.current), delay: preset.pokeDwell, activity: null, state: 'poke' }
        }
        if (pinnedRef.current !== null) {
          const fixed = BY_FILE.get(pinnedRef.current)
          if (fixed) return { asset: fixed, delay: 60000, activity: null, state: 'pin' }
        }
        if (draggingRef.current) {
          // 被拎着：全程只播抽中的那一个，拖着的时候不换动作。
          const held = machine.holdAsset || pick(pool(pet, 'hold'), assetRef.current)
          return { asset: held, delay: 3000, activity: null, state: 'hold' }
        }
        if (now < machine.dropUntil && machine.dropAsset) {
          // 刚被放下：同样只播抽中的那一个，播完这段就回去过日子。
          return {
            asset: machine.dropAsset,
            delay: Math.max(250, machine.dropUntil - now),
            activity: null,
            state: 'drop',
          }
        }
        if (now < machine.celebrateUntil) {
          return {
            asset: pick(pool(pet, 'celebrate'), assetRef.current),
            delay: preset.celebrateDwell,
            activity: null,
            state: 'celebrate',
          }
        }

        const phase = phaseRef.current
        if (phase === 'attention') {
          return {
            asset: pick(pool(pet, 'attention'), assetRef.current),
            delay: preset.busyDwell + 1500,
            activity: null,
            state: 'attention',
            say: 'attention',
          }
        }
        if (phase === 'running') {
          // 干活期间锁定一个动作：开工先「思考」，THINK_LEAD_MS 之后只切一次到「干活」，
          // 然后整个任务都用同一个动作，不再换。
          if (machine.runAsset === null) {
            machine.runAsset = pick(pool(pet, 'think'), assetRef.current)
            machine.runPhase = 'think'
          }
          const elapsed = now - machine.runningSince
          if (machine.runPhase === 'think' && elapsed >= THINK_LEAD_MS) {
            machine.runAsset = pick(pool(pet, 'work'), machine.runAsset)
            machine.runPhase = 'work'
          }
          return {
            asset: machine.runAsset,
            delay: machine.runPhase === 'think' ? Math.max(400, THINK_LEAD_MS - elapsed) : 60000,
            activity: null,
            state: 'running',
            say: machine.runPhase,
          }
        }
        if (phase === 'done' && now - machine.doneSince < 25000) {
          // 后台干完活先提醒一阵子，然后就回去过日子，不能一直举着铃铛。
          return {
            asset: pick(pool(pet, 'notify'), assetRef.current),
            delay: preset.busyDwell + 2000,
            activity: null,
            state: 'done',
            say: 'done',
          }
        }

        // 没事干：过一段「生活」，而不是一首一首换歌。
        if (machine.activity === null || now >= machine.activityUntil) {
          const candidates = ACTIVITIES.filter(
            (entry) => entry.pool !== machine.activity && pool(pet, entry.pool).length > 0,
          )
          const chosen = candidates.length > 0 ? candidates[Math.floor(Math.random() * candidates.length)] : ACTIVITIES[0]
          const span = preset.activityMin + Math.random() * Math.max(0, preset.activityMax - preset.activityMin)
          machine.activity = chosen.pool
          machine.activityUntil = now + span * chosen.scale
          // 换生活的时候有 1/4 概率不说这件事，而是自言自语一句。
          machine.activitySay = Math.random() < 0.25 ? 'muse' : chosen.pool
        }
        return {
          asset: pick(pool(pet, machine.activity), assetRef.current),
          delay: preset.ambientDwell,
          activity: machine.activity,
          state: 'idle',
          say: machine.activitySay || machine.activity,
        }
      }, [])

      // 干活结束的那一下：庆祝几秒（等你回答不算「结束」）。
      React.useEffect(() => {
        const previous = previousPhaseRef.current
        previousPhaseRef.current = worldPhase
        if (worldPhase === 'running') {
          if (previous !== 'running') {
            machineRef.current.runningSince = Date.now()
            // 新的任务 = 新的动作抽签（整段任务只用一个）+ 记下开工时的累计用量。
            machineRef.current.runAsset = null
            machineRef.current.runPhase = null
            machineRef.current.usageAtStart = { ...tokenRef.current }
          }
          return
        }
        if (previous === 'running') {
          // 收工：把干活动作清掉，下次开工重新抽。
          machineRef.current.runAsset = null
          machineRef.current.runPhase = null
        }
        if (worldPhase === 'done' && previous !== 'done') {
          machineRef.current.doneSince = Date.now()
          return
        }
        if (previous === 'running' && (worldPhase === 'idle' || worldPhase === 'done')) {
          machineRef.current.celebrateUntil = Date.now() + 6000
          setSignal((n) => n + 1)
          speak('celebrate', { force: true })

          // 收工汇报：这次花了多久、写了多少 token。
          // 让庆祝那句先露个脸，4.6 秒后再报数据。
          const seconds = (Date.now() - machineRef.current.runningSince) / 1000
          const start = machineRef.current.usageAtStart || { total: 0, output: 0 }
          const outputDelta = Math.max(0, tokenRef.current.output - start.output)
          const totalDelta = Math.max(0, tokenRef.current.total - start.total)
          const report =
            outputDelta > 0
              ? { pool: 'report', vars: { t: formatDuration(seconds), o: formatTokens(outputDelta), a: formatTokens(totalDelta) } }
              : { pool: 'reportTime', vars: { t: formatDuration(seconds) } }
          if (reportTimerRef.current) reportTimerRef.current()
          reportTimerRef.current = scheduleOnce(() => {
            reportTimerRef.current = null
            speak(report.pool, { force: true, vars: report.vars })
          }, 4600)
        }
      }, [worldPhase, speak, scheduleOnce])

      // 组件卸载时把还没响的汇报定时器清掉。
      React.useEffect(
        () => () => {
          if (reportTimerRef.current) reportTimerRef.current()
        },
        [],
      )

      // 状态机主循环：算一步、播一步、排下一步。输入一变就立刻重算。
      React.useEffect(() => {
        if (prefs.hidden) return undefined
        // 一个动画都没有时不空转（花名册到位/导入成功后会因为 rosterSeq 变化重启）。
        if (catalog.assets.length === 0) return undefined
        let cancelled = false
        let dispose = null
        const schedule = (fn, ms) => {
          if (timer && typeof timer.timeout === 'function') dispose = timer.timeout(fn, ms)
          else {
            const id = window.setTimeout(fn, ms)
            dispose = () => window.clearTimeout(id)
          }
        }
        const step = () => {
          if (cancelled) return
          const decision = plan()
          // 只有真的换动画才重挂 <img>：同一个动作重新挂载会把 GIF 从头播。
          if (decision.asset.f !== assetRef.current.f) setToken((n) => n + 1)
          assetRef.current = decision.asset
          setAsset(decision.asset)
          setActivity(decision.activity)
          setMachineState(decision.state)
          // 只在「该说什么」真的变了的时候开口，所以状态内换动作不会一直叨叨。
          if (decision.say) {
            if (decision.say !== sayRef.current) {
              const summary = worldRef.current
              speak(decision.say, { n: decision.say === 'attention' ? summary.waiting : summary.total })
            }
            sayRef.current = decision.say
          } else {
            sayRef.current = null
          }
          schedule(step, decision.delay)
        }
        step()
        return () => {
          cancelled = true
          if (dispose) dispose()
        }
      }, [plan, timer, prefs.hidden, prefs.rate, prefs.talk, prefs.pet, signal, pinned, dragging, worldPhase, speak, rosterSeq])

      // 窗口变小的时候别把鱼留在屏幕外。
      React.useEffect(() => {
        const onResize = () => {
          setPrefs((current) => {
            const next = {
              ...current,
              x: clamp(current.x, 0, Math.max(0, window.innerWidth - current.size - 4)),
              y: clamp(current.y, 0, Math.max(0, window.innerHeight - current.size - 4)),
            }
            savePrefs(next)
            return next
          })
        }
        window.addEventListener('resize', onResize)
        return () => window.removeEventListener('resize', onResize)
      }, [])

      const onPointerDown = React.useCallback(
        (event) => {
          if (event.button !== 0) return
          event.preventDefault()
          const startX = event.clientX
          const startY = event.clientY
          const base = { x: prefsRef.current.x, y: prefsRef.current.y }
          let moved = false
          const move = (moveEvent) => {
            const dx = moveEvent.clientX - startX
            const dy = moveEvent.clientY - startY
            if (!moved && Math.abs(dx) + Math.abs(dy) > 5) {
              moved = true
              // 拎起来的那一刻抽一个动作，整段拖拽就播它。
              machineRef.current.holdAsset = pick(pool(prefsRef.current.pet, 'hold'), assetRef.current)
              setDragging(true)
              speak('hold', { force: true })
            }
            if (!moved) return
            const size = prefsRef.current.size
            update({
              x: clamp(base.x - dx, 0, Math.max(0, window.innerWidth - size - 4)),
              y: clamp(base.y - dy, 0, Math.max(0, window.innerHeight - size - 4)),
            })
          }
          const finish = (upEvent) => {
            window.removeEventListener('pointermove', move)
            window.removeEventListener('pointerup', finish)
            window.removeEventListener('pointercancel', finish)
            setDragging(false)
            if (moved) {
              // 放下也只播一个动作：抽一个落地反应，播完它自己那一轮就结束。
              const landed = pick(pool(prefsRef.current.pet, 'drop'), machineRef.current.holdAsset)
              machineRef.current.dropAsset = landed
              machineRef.current.dropUntil = Date.now() + Math.max(DROP_MIN_MS, landed.ms || 1600)
              setSignal((n) => n + 1)
              speak('drop', { force: true })
              return
            }
            if (upEvent && upEvent.detail >= 2) return // 双击由 onDoubleClick 处理
            poke()
          }
          window.addEventListener('pointermove', move)
          window.addEventListener('pointerup', finish)
          window.addEventListener('pointercancel', finish)
        },
        [poke, update, speak],
      )

      const size = prefs.size

      if (prefs.hidden) {
        return h(
          'div',
          { style: { position: 'fixed', right: 14, bottom: 104, zIndex: 60, pointerEvents: 'auto' } },
          h(
            'button',
            {
              type: 'button',
              title: t('show'),
              onClick: () => {
                update({ hidden: false })
                speak('show', { force: true, reveal: true })
              },
              style: {
                width: 40,
                height: 40,
                borderRadius: 20,
                border: '1px solid var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
                background: 'var(--dsw-alias-bg-overlay, rgba(255,255,255,0.9))',
                boxShadow: '0 4px 12px rgba(0,0,0,0.22)',
                cursor: 'pointer',
                fontSize: 20,
                lineHeight: 1,
              },
            },
            '🐟',
          ),
        )
      }

      const pet = h(
        'div',
        {
          onPointerDown,
          onDoubleClick: (event) => {
            event.preventDefault()
            setPanelOpen((open) => !open)
          },
          onContextMenu: (event) => {
            event.preventDefault()
            setPanelOpen((open) => !open)
          },
          title: `${asset.n} · ${t('hint')}`,
          style: {
            position: 'fixed',
            right: prefs.x,
            bottom: prefs.y,
            width: size,
            height: size,
            zIndex: 60,
            pointerEvents: 'auto',
            cursor: dragging ? 'grabbing' : 'grab',
            touchAction: 'none',
            userSelect: 'none',
            // 被拎着就歪着、稍微放大一点；放下时播一下落地回弹。
            transform: dragging ? 'rotate(-6deg) scale(1.06)' : 'rotate(0deg) scale(1)',
            transformOrigin: '50% 90%',
            animation: machineState === 'drop' ? 'dsh-fish-pet-drop 420ms ease' : 'none',
            transition: dragging ? 'transform 120ms ease' : 'right 140ms ease, bottom 140ms ease, transform 180ms ease',
          },
        },
        h('img', {
          key: `${asset.f}#${token}`,
          src: srcOf(asset),
          alt: asset.n,
          draggable: false,
          onError: () => setImageFailures((n) => n + 1),
          style: {
            width: '100%',
            height: '100%',
            objectFit: 'contain',
            display: 'block',
            pointerEvents: 'none',
            filter: 'drop-shadow(0 8px 12px rgba(0,0,0,0.28))',
          },
        }),
      )

      const panelWidth = 306
      // 面板和气泡各自占用鱼的一侧：面板优先放上方，气泡就自动让到另一侧，互不遮挡。
      // （之前只按「窗口能不能装下」来限位，结果把面板压到窗口底部，正好盖住鱼。）
      const panelGap = 8
      const petTop = window.innerHeight - prefs.y - size
      const spaceAbove = Math.max(0, petTop - panelGap - 12)
      const spaceBelow = Math.max(0, prefs.y - panelGap - 12)
      const placeAbove = spaceAbove >= spaceBelow
      const sideSpace = placeAbove ? spaceAbove : spaceBelow
      const panelMaxHeight = Math.max(160, Math.min(540, sideSpace))
      const panelVertical = placeAbove ? { bottom: prefs.y + size + panelGap } : { top: petTop + panelGap }
      const panelRight = clamp(prefs.x + size / 2 - panelWidth + 56, 10, Math.max(10, window.innerWidth - panelWidth - 10))
      const keyword = query.trim().toLowerCase()
      // 注意：这里在 hidden 的提前 return 之后，所以只能用普通计算，不能上 useMemo（hooks 顺序）。
      const activeAssets = petAssets(prefs.pet)
      const visible =
        keyword === '' ? activeAssets : activeAssets.filter((item) => item.n.toLowerCase().includes(keyword))
      // res/ 里还没导入的压缩包（只有 Host 半边给了运行时花名册才知道）。
      const resArchives = catalog.res && Array.isArray(catalog.res.archives) ? catalog.res.archives : []
      const pendingArchives = catalog.res && Array.isArray(catalog.res.pending) ? catalog.res.pending : []
      const readyArchives = resArchives.filter((item) => item.pet !== '')
      const noAssets = catalog.assets.length === 0

      // 干活时常驻的状态条：干活中/等你回答，其余时间只有说话时才出现。
      const stickyKey = worldPhase === 'running' ? 'working' : worldPhase === 'attention' ? 'waiting' : null
      const elapsedSeconds =
        worldPhase === 'running' ? Math.max(0, Math.floor((Date.now() - machineRef.current.runningSince) / 1000)) : 0
      void beat

      // 气泡：默认在鱼上方；面板占了上方时它就去下方。
      const bubbleAbove = panelOpen ? !placeAbove : true
      // 情绪决定描边颜色；color-mix 不被支持时退回普通描边色。
      const bubbleTint = bubble && bubble.mood ? MOOD_TINT[bubble.mood] : null
      const bubbleBorderColor = bubbleTint
        ? `color-mix(in srgb, ${bubbleTint} 60%, var(--dsw-alias-border-l1, rgba(127,127,127,0.35)))`
        : 'var(--dsw-alias-border-l1, rgba(127,127,127,0.35))'
      // 连续 3 次图片加载失败 → 占用气泡位置显示一条说明（带重扫/导入按钮，可以关掉）。
      const smallButtonStyle = {
        font: 'inherit',
        fontSize: 10,
        padding: '1px 5px',
        borderRadius: 6,
        border: '1px solid var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
        background: 'transparent',
        color: 'inherit',
        cursor: 'pointer',
      }
      const noticeNode =
        noticeHidden || imageFailures < 3
          ? null
          : h(
              'div',
              { style: { ...NOTICE_STYLE, right: clamp(prefs.x, 8, Math.max(8, window.innerWidth - 260)), bottom: prefs.y + size + 6 } },
              [
                h('div', { key: 'text' }, `🐟 ${t('loadFail')} —— ${t('buildHint')}`),
                h('div', { key: 'row', style: { marginTop: 4, display: 'flex', gap: 4, flexWrap: 'wrap' } }, [
                  h(
                    'button',
                    {
                      key: 'rescan',
                      type: 'button',
                      disabled: rosterState === 'loading',
                      onClick: () => {
                        setImageFailures(0)
                        void refreshRoster({ force: true, loud: true })
                      },
                      style: smallButtonStyle,
                    },
                    `⟳ ${t('rescan')}`,
                  ),
                  h(
                    'button',
                    { key: 'import', type: 'button', disabled: importing, onClick: () => void importFromRes(false), style: smallButtonStyle },
                    importing ? t('importing') : `${t('import')} ${t('archive')}`,
                  ),
                  h(
                    'button',
                    {
                      key: 'close',
                      type: 'button',
                      onClick: () => setNoticeHidden(true),
                      style: smallButtonStyle,
                    },
                    '✕',
                  ),
                ]),
              ],
            )
      const bubbleNode =
        noticeNode !== null
          ? noticeNode
          : bubble === null && stickyKey === null
          ? null
          : h(
              'div',
              {
                onClick: () => speak(sayRef.current || 'muse', { force: true }),
                title: t('sayOne'),
                style: {
                  position: 'fixed',
                  right: clamp(prefs.x, 8, Math.max(8, window.innerWidth - 260)),
                  ...(bubbleAbove ? { bottom: prefs.y + size + 6 } : { top: window.innerHeight - prefs.y + 6 }),
                  maxWidth: 240,
                  zIndex: 62,
                  pointerEvents: 'auto',
                  cursor: 'pointer',
                  padding: '6px 10px',
                  borderRadius: 10,
                  border: `1px solid ${bubbleBorderColor}`,
                  background: 'var(--dsw-alias-bg-overlay, rgba(255,255,255,0.97))',
                  color: 'var(--dsw-alias-label-primary, inherit)',
                  boxShadow: '0 6px 18px rgba(0,0,0,0.18)',
                  fontSize: 12,
                  lineHeight: 1.45,
                  whiteSpace: 'pre-wrap',
                  wordBreak: 'break-word',
                },
              },
              [
                stickyKey === null
                  ? null
                  : h(
                      'div',
                      {
                        key: 'status',
                        style: {
                          display: 'flex',
                          alignItems: 'center',
                          gap: 5,
                          fontSize: 11,
                          fontWeight: 600,
                          color: 'var(--dsw-alias-brand-primary, inherit)',
                        },
                      },
                      h('span', {
                        key: 'dot',
                        style: {
                          display: 'inline-block',
                          width: 6,
                          height: 6,
                          borderRadius: 3,
                          background: 'var(--dsw-alias-brand-primary, #4a8)',
                          animation: 'dsh-fish-pet-pulse 1.4s ease-in-out infinite',
                        },
                      }),
                      t(stickyKey),
                      stickyKey === 'working' ? ` ${elapsedSeconds}s` : null,
                    ),
                bubble === null
                  ? null
                  : h(
                      'div',
                      { key: 'text', style: { marginTop: stickyKey === null ? 0 : 4 } },
                      bubble.text,
                    ),
                h('div', {
                  key: 'tail',
                  style: {
                    position: 'absolute',
                    right: 16,
                    [bubbleAbove ? 'bottom' : 'top']: -5,
                    width: 10,
                    height: 10,
                    transform: 'rotate(45deg)',
                    background: 'var(--dsw-alias-bg-overlay, rgba(255,255,255,0.97))',
                    ...(bubbleAbove
                      ? { borderRight: `1px solid ${bubbleBorderColor}`, borderBottom: `1px solid ${bubbleBorderColor}` }
                      : { borderLeft: `1px solid ${bubbleBorderColor}`, borderTop: `1px solid ${bubbleBorderColor}` }),
                  },
                }),
              ],
            )

      const buttonStyle = {
        font: 'inherit',
        fontSize: 11,
        padding: '3px 8px',
        borderRadius: 7,
        border: '1px solid var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
        background: 'var(--dsw-alias-bg-layer-1, rgba(127,127,127,0.08))',
        color: 'var(--dsw-alias-label-primary, inherit)',
        cursor: 'pointer',
        whiteSpace: 'nowrap',
      }

      const panel = h(
        'div',
        {
          style: {
            position: 'fixed',
            right: panelRight,
            ...panelVertical,
            width: panelWidth,
            maxHeight: panelMaxHeight,
            zIndex: 61,
            pointerEvents: 'auto',
            padding: 10,
            borderRadius: 12,
            border: '1px solid var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
            background: 'var(--dsw-alias-bg-overlay, rgba(255,255,255,0.97))',
            color: 'var(--dsw-alias-label-primary, inherit)',
            boxShadow: '0 12px 32px rgba(0,0,0,0.30)',
            display: 'flex',
            flexDirection: 'column',
            gap: 6,
            overflow: 'hidden',
          },
        },
        h(
          'div',
          { style: { display: 'flex', alignItems: 'center', gap: 4, flex: '0 0 auto' } },
          h('strong', { style: { fontSize: 12, flex: '1 1 auto' } }, t('title')),
          h(
            'button',
            {
              type: 'button',
              onClick: () => setShowDiag((open) => !open),
              style: { ...buttonStyle, padding: '1px 7px' },
              title: t('diag'),
            },
            'i',
          ),
          h(
            'button',
            {
              type: 'button',
              onClick: () => setPanelOpen(false),
              style: { ...buttonStyle, padding: '1px 7px' },
              title: t('hide'),
            },
            '✕',
          ),
        ),
        h(
          'div',
          { style: { fontSize: 11, color: 'var(--dsw-alias-label-secondary, inherit)', flex: '0 0 auto' } },
          `${t('state')}：${t(worldPhase)} · ${t('pet')}：${t(
            activity !== null ? activity : machineState === 'poke' ? 'pokeState' : machineState,
          )}`,
        ),
        // 宠物行常驻：只有一只的时候也要让用户看见「在哪加新宠物」。
        h(
          'div',
          { style: { display: 'flex', alignItems: 'center', gap: 6, fontSize: 11, flex: '0 0 auto', flexWrap: 'wrap' } },
          h('span', { style: { whiteSpace: 'nowrap' } }, t('petSwap')),
          h(
            'div',
            { style: { display: 'flex', flexWrap: 'wrap', gap: 4 } },
            catalog.pets.map((pet) =>
              h(
                'button',
                {
                  key: pet.id,
                  type: 'button',
                  title: `${pet.id} · ${pet.count} ${t('total')}`,
                  onClick: () => switchPet(pet.id),
                  style: {
                    ...buttonStyle,
                    borderColor:
                      prefs.pet === pet.id
                        ? 'var(--dsw-alias-brand-primary, rgba(80,140,255,0.9))'
                        : 'var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
                    color: prefs.pet === pet.id ? 'var(--dsw-alias-brand-primary, inherit)' : 'inherit',
                    fontWeight: prefs.pet === pet.id ? 600 : 400,
                  },
                },
                pet.label,
              ),
            ),
            noAssets
              ? h(
                  'span',
                  { style: { color: 'var(--dsw-alias-label-secondary, inherit)' } },
                  t('noAssets'),
                )
              : null,
          ),
          h(
            'button',
            {
              type: 'button',
              title: t('rescanTitle'),
              disabled: rosterState === 'loading',
              onClick: () => void refreshRoster({ force: true, loud: true }),
              style: { ...buttonStyle, opacity: rosterState === 'loading' ? 0.55 : 1 },
            },
            `⟳ ${t('rescan')}`,
          ),
          h(
            'button',
            {
              type: 'button',
              title: t('importTitle'),
              disabled: importing,
              onClick: () => void importFromRes(false),
              style: {
                ...buttonStyle,
                opacity: importing ? 0.55 : 1,
                borderColor:
                  pendingArchives.length > 0
                    ? 'var(--dsw-alias-brand-primary, rgba(80,140,255,0.9))'
                    : 'var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
                color: pendingArchives.length > 0 ? 'var(--dsw-alias-brand-primary, inherit)' : 'inherit',
                fontWeight: pendingArchives.length > 0 ? 600 : 400,
              },
            },
            importing
              ? t('importing')
              : `${t('import')} ${t('archive')}${pendingArchives.length > 0 ? ` (${pendingArchives.length})` : ''}`,
          ),
          readyArchives.length > 0 && pendingArchives.length === 0
            ? h(
                'button',
                {
                  type: 'button',
                  title: t('importForceTitle'),
                  disabled: importing,
                  onClick: () => void importFromRes(true),
                  style: { ...buttonStyle, opacity: importing ? 0.55 : 1 },
                },
                t('importForce'),
              )
            : null,
        ),
        h(
          'div',
          { style: { fontSize: 10, color: 'var(--dsw-alias-label-secondary, inherit)', flex: '0 0 auto' } },
          rosterNote !== null
            ? rosterNote
            : `${t('petHint')}（${t('roster')}：${
                catalog.source === 'runtime' ? t('rosterRuntime') : t('rosterBuilt')
              }${catalog.pets.length > 0 ? ` · ${catalog.pets.length} ${t('total')}` : ''}）`,
        ),
        showDiag
          ? h(
              'div',
              { style: { fontSize: 10, color: 'var(--dsw-alias-label-secondary, inherit)', flex: '0 0 auto' } },
              `${t('diag')}：${t(sourceKey)} · ${t('sessions')}${world.total} · ${t('sRun')}${world.running} · ${t(
                'sWait',
              )}${world.waiting} · ${t('sDone')}${world.finished} · ${t('pets')}${petLabel(prefs.pet)}(${
                activeAssets.length
              }) · ${t('roster')}${catalog.source === 'runtime' ? t('rosterRuntime') : t('rosterBuilt')}/${rosterState}${
                catalog.scanMs > 0 ? ` ${catalog.scanMs}ms` : ''
              } · ${t('archive')}${readyArchives.length}(-${pendingArchives.length}) · dict:${localeInfo()} · ${formatTokens(
                tokenTotal,
              )} ${t('sTokens')} · ${t('sEvents')}${binding ? eventCountRef.current : '×'}`,
            )
          : null,
        h(
          'label',
          { style: { display: 'flex', alignItems: 'center', gap: 6, fontSize: 11, flex: '0 0 auto' } },
          h('span', { style: { whiteSpace: 'nowrap' } }, t('size')),
          h('input', {
            type: 'range',
            min: MIN_SIZE,
            max: MAX_SIZE,
            step: 10,
            value: size,
            onChange: (event) => update({ size: clamp(Number(event.target.value), MIN_SIZE, MAX_SIZE) }),
            style: { flex: '1 1 auto' },
          }),
          h('span', { style: { width: 40, textAlign: 'right' } }, `${size}px`),
        ),
        h(
          'div',
          { style: { display: 'flex', alignItems: 'center', gap: 6, fontSize: 11, flex: '0 0 auto' } },
          h('span', { style: { whiteSpace: 'nowrap' } }, t('rate')),
          h(
            'div',
            { style: { display: 'flex', gap: 4 } },
            RATE_ORDER.map((id) =>
              h(
                'button',
                {
                  key: id,
                  type: 'button',
                  onClick: () => update({ rate: id }),
                  style: {
                    ...buttonStyle,
                    // 选中态只用品牌色描边 + 加粗：直接拿 brand 当底色会在深色主题里变成黑底黑字。
                    borderColor:
                      prefs.rate === id
                        ? 'var(--dsw-alias-brand-primary, rgba(80,140,255,0.9))'
                        : 'var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
                    color: prefs.rate === id ? 'var(--dsw-alias-brand-primary, inherit)' : 'inherit',
                    fontWeight: prefs.rate === id ? 600 : 400,
                  },
                },
                t(id),
              ),
            ),
          ),
        ),
        h(
          'div',
          { style: { display: 'flex', flexWrap: 'wrap', gap: 6, flex: '0 0 auto' } },
          h('button', { type: 'button', style: buttonStyle, onClick: poke }, t('poke')),
          h(
            'button',
            {
              type: 'button',
              style: buttonStyle,
              onClick: () => speak(sayRef.current || 'muse', { force: true }),
            },
            t('sayOne'),
          ),
          h(
            'button',
            {
              type: 'button',
              onClick: () => update({ talk: prefs.talk === false }),
              style: {
                ...buttonStyle,
                borderColor:
                  prefs.talk === false
                    ? 'var(--dsw-alias-border-l1, rgba(127,127,127,0.35))'
                    : 'var(--dsw-alias-brand-primary, rgba(80,140,255,0.9))',
                color: prefs.talk === false ? 'inherit' : 'var(--dsw-alias-brand-primary, inherit)',
                fontWeight: prefs.talk === false ? 400 : 600,
              },
            },
            `${t('talk')}：${prefs.talk === false ? t('talkOff') : t('talkOn')}`,
          ),
          pinned === null
            ? null
            : h('button', { type: 'button', style: buttonStyle, onClick: () => setPinned(null) }, t('auto')),
          h('button', { type: 'button', style: buttonStyle, onClick: () => update({ hidden: true }) }, t('hide')),
          h(
            'button',
            {
              type: 'button',
              style: buttonStyle,
              onClick: () => update({ x: DEFAULT_PREFS.x, y: DEFAULT_PREFS.y }),
            },
            t('reset'),
          ),
        ),
        h('input', {
          type: 'search',
          value: query,
          placeholder: `${t('search')} (${activeAssets.length})`,
          onChange: (event) => setQuery(event.target.value),
          style: {
            font: 'inherit',
            fontSize: 11,
            padding: '4px 7px',
            borderRadius: 7,
            border: '1px solid var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
            background: 'var(--dsw-alias-bg-layer-1, rgba(127,127,127,0.08))',
            color: 'inherit',
            flex: '0 0 auto',
          },
        }),
        h(
          'div',
          {
            style: {
              flex: '1 1 auto',
              // 固定行高 + 自己的滚动条：列表只会滚动，不会被 flex 压扁成一条条细缝。
              minHeight: 120,
              overflowY: 'auto',
              overflowX: 'hidden',
              display: 'grid',
              // minmax(0, 1fr)：不加这个，nowrap 的按钮会把轨道撑成 min-content 宽度。
              gridTemplateColumns: 'minmax(0, 1fr) minmax(0, 1fr)',
              gridAutoRows: '24px',
              gap: 4,
              alignContent: 'start',
              padding: 4,
              borderRadius: 8,
              background: 'var(--dsw-alias-bg-layer-1, rgba(127,127,127,0.06))',
            },
          },
          visible.length === 0
            ? h(
                'div',
                {
                  style: {
                    gridColumn: '1 / -1',
                    fontSize: 11,
                    padding: '6px 4px',
                    color: 'var(--dsw-alias-label-secondary, inherit)',
                  },
                },
                t('noMatch'),
              )
            : visible.map((item) =>
                h(
                  'button',
                  {
                    key: item.f,
                    type: 'button',
                    title: item.f,
                    onClick: () => {
                  setPinned(item.f)
                  speak('pin', { force: true })
                },
                    style: {
                      ...buttonStyle,
                      boxSizing: 'border-box',
                      height: 24,
                      lineHeight: '22px',
                      padding: '0 7px',
                      minWidth: 0,
                      textAlign: 'left',
                      overflow: 'hidden',
                      textOverflow: 'ellipsis',
                      borderColor:
                        pinned === item.f || asset.f === item.f
                          ? 'var(--dsw-alias-brand-primary, rgba(80,140,255,0.9))'
                          : 'var(--dsw-alias-border-l1, rgba(127,127,127,0.35))',
                      color: pinned === item.f ? 'var(--dsw-alias-brand-primary, inherit)' : 'inherit',
                      fontWeight: pinned === item.f ? 600 : 400,
                    },
                  },
                  item.n,
                ),
              ),
        ),
      )

      // 一个动画都没有（新克隆还没导入素材 / 素材被删了）：给一张能自救的卡片。
      // 面板本来要靠点宠物才打得开，这时候必须让按钮直接出现在卡片上。
      if (noAssets) {
        return h(
          'div',
          { style: { ...NOTICE_STYLE, display: 'flex', flexDirection: 'column', gap: 6, maxWidth: 280 } },
          h('div', { key: 'title', style: { fontWeight: 600 } }, `🐟 ${t('noAssets')}`),
          h('div', { key: 'hint' }, t('noAssetsHint')),
          h('div', { key: 'row', style: { display: 'flex', gap: 6, flexWrap: 'wrap' } }, [
            h(
              'button',
              {
                key: 'import',
                type: 'button',
                disabled: importing,
                onClick: () => void importFromRes(false),
                style: { ...buttonStyle, opacity: importing ? 0.55 : 1 },
              },
              importing ? t('importing') : `${t('import')} ${t('archive')}`,
            ),
            h(
              'button',
              { key: 'rescan', type: 'button', disabled: rosterState === 'loading', onClick: () => void refreshRoster({ force: true, loud: true }), style: { ...buttonStyle, opacity: rosterState === 'loading' ? 0.55 : 1 } },
              `⟳ ${t('rescan')}`,
            ),
          ]),
          rosterNote !== null
            ? h('div', { key: 'note', style: { fontSize: 10, color: 'var(--dsw-alias-label-secondary, inherit)' } }, rosterNote)
            : null,
        )
      }

      return h(React.Fragment, null, pet, bubbleNode, panelOpen ? panel : null)
    }

    return {
      inject: ['slots'],
      apply(ctx) {
        /**
         * 词典。
         *
         * 客户端插件是 `immediately` 挂载的，apply() 执行这一刻 `ctx.get('locale')`
         * 很可能还没准备好 —— 老写法「拿一次，拿不到就一辈子回退成原始 key」的症状
         * 就是面板上写着 petSwap / size / poke 这种原始 key（实测踩过）。
         * 所以这里不再在 apply 时定死，而是：
         *   - 登记与绑定都做成懒的：t() 每次被调用时再看一眼 service 在不在；
         *   - 同时用 locale/change 事件 + 两次短延时重试，兜住「服务晚到、之后又不重渲染」的情况。
         * bind() 返回的函数每次调用都现读语言与词典，所以绑一次之后切语言照样即时生效。
         */
        const localeState = { service: null, bound: null, registered: false, ids: new Set() }
        const registeredIds = new Set()
        const disposeRegistrations = []

        /** 服务在就（幂等地）登记词典；返回可用的服务本身。 */
        const ensureLocale = () => {
          const service = localeState.service || ctx.get('locale')
          if (!service || typeof service.bind !== 'function') return null
          localeState.service = service
          if (!localeState.registered) {
            localeState.registered = true
            // 写死 zh-CN/en 不够：真实 id 可能是 zh-Hans / zh 之类，一变就查不到。
            for (const id of ['zh-CN', 'zh-Hans', 'zh', 'en']) localeState.ids.add(id)
            try {
              const snapshot = typeof service.getLocale === 'function' ? service.getLocale() : null
              if (snapshot && Array.isArray(snapshot.locales)) {
                for (const definition of snapshot.locales) {
                  if (definition && typeof definition.id === 'string') localeState.ids.add(definition.id)
                }
              }
            } catch {
              /* 读不到语言表就用上面几个兜底 id */
            }
            for (const id of localeState.ids) {
              if (registeredIds.has(id)) continue
              try {
                const dispose = service.register('fish-pet', id, /^zh/i.test(id) ? DICT['zh-CN'] : DICT.en)
                registeredIds.add(id)
                if (typeof dispose === 'function') disposeRegistrations.push(dispose)
              } catch (error) {
                console.error(`dsh-fish-pet: locale dict for ${id} failed`, error)
              }
            }
          }
          return service
        }

        /** 稳定的翻译入口：服务晚到也能补上；绑一次之后不再重复绑。 */
        const t = (key, vars) => {
          if (localeState.bound === null) {
            const service = ensureLocale()
            if (service === null) return key
            try {
              localeState.bound = service.bind('fish-pet')
            } catch (error) {
              console.error('dsh-fish-pet: locale bind failed', error)
              return key
            }
          }
          return localeState.bound(key, vars)
        }

        // 挂载时先登记一次；服务晚到就靠事件 + 两次短延时补上。
        ctx.effect(() => {
          ensureLocale()
          let off = null
          try {
            if (typeof ctx.on === 'function') off = ctx.on('locale/change', () => ensureLocale())
          } catch {
            /* 没有这个事件也没关系，下面还有延时兜底 */
          }
          const timers = [300, 1500].map((ms) => window.setTimeout(() => ensureLocale(), ms))
          return () => {
            if (typeof off === 'function') off()
            for (const id of timers) window.clearTimeout(id)
            for (const dispose of disposeRegistrations.splice(0)) {
              try {
                dispose()
              } catch {
                /* 已经移除过 */
              }
            }
            localeState.registered = false
            localeState.bound = null
          }
        })

        const timer = ctx.get('timer')

        // 被拎住/放下的那点表现力，用一次性的样式表；插件停掉时自动移除。
        if (typeof styles !== 'undefined' && styles && typeof styles.insert === 'function') {
          ctx.effect(() => styles.insert(PET_CSS))
        }

        // 兜底路线：直接订阅会话服务的 sessionStatus（getSnapshot/subscribe 对）。
        const uiSession = ctx.get('uiSession')
        const sessionStatus = uiSession && uiSession.sessionStatus
        const statusStore =
          sessionStatus &&
          typeof sessionStatus.getSnapshot === 'function' &&
          typeof sessionStatus.subscribe === 'function'
            ? {
                read: () => worldSummaryOf(sessionStatus.getSnapshot()),
                subscribe: (notify) => sessionStatus.subscribe(notify),
              }
            : null

        // 台词表现读当前语言，切换语言立刻生效。
        const activeLocale = () => {
          const service = ensureLocale()
          if (!service) return 'zh'
          try {
            const snapshot = typeof service.getLocale === 'function' ? service.getLocale() : null
            return snapshot && typeof snapshot.active === 'string' ? snapshot.active : 'zh'
          } catch {
            return 'zh'
          }
        }

        /** 诊断行里显示词典状态：`none` = 服务还没来，`unbound` = 没绑上，`nodict` = 没登记上。 */
        const localeInfo = () => {
          const service = ctx.get('locale')
          if (!service) return 'none'
          let active = '?'
          try {
            const snapshot = service.getLocale()
            active = snapshot && typeof snapshot.active === 'string' ? snapshot.active : '?'
          } catch {
            active = 'err'
          }
          return `${active}${localeState.registered ? '' : '·nodict'}${localeState.bound === null ? '·unbound' : ''}`
        }

        // 会话事件流（干活播报用）：客户端会话控制器给的 binding.eventSource。
        const sessionsService = ctx.get('sessions')
        const Root = (slotProps) =>
          h(
            PetBoundary,
            null,
            h(FishPet, { ...slotProps, timer, t, statusStore, activeLocale, localeInfo, sessionsService }),
          )
        ctx.effect(() =>
          ctx.slots.inject('shell.overlay', () =>
            ctx.slots.register(
              { name: 'shell.overlay', id: 'dsh-fish-pet', order: 50, label: () => t('label') },
              Root,
            ),
          ),
        )
      },
    }
  },
})
